# APL253 × NL ↔ [AB | DE | SD] — Complete Digital-Twin Specification

## Formal Grammar

The translation between Natural Language pattern descriptions (NL) and multi-paradigm simulation primitives follows a compositional grammar where each Alexander pattern is expressed as a typed production rule mapping spatial/social/temporal intent to one or more simulation paradigm operators.

### Notation

```
Pattern(id) ::= NL_description → Paradigm_Expression

Paradigm_Expression ::=
  | AB(agent_type, state_machine, spatial_rule)
  | DE(event_type, queue_priority, process_flow)
  | SD(stock, flow_rate, feedback_polarity)
  | AB ⊗ DE(agent_event_coupling)
  | AB ⊗ SD(agent_stock_coupling)
  | DE ⊗ SD(event_flow_coupling)
  | AB ⊗ DE ⊗ SD(full_coupling)

where:
  ⊗  = tensorial composition (interaction/entanglement)
  ⊕  = additive composition (choice/superposition)
  →  = causal production
  ↔  = bidirectional coupling
  ∂  = partial differential (SD rate)
  Δ  = discrete state transition (DE event)
  σ  = agent state function (AB rule)
```

### Paradigm Primitives

| Primitive | Paradigm | Semantics |
|-----------|----------|-----------|
| `spawn(type, pos, state)` | AB | Create agent at location |
| `move(agent, heading, speed)` | AB | Agent locomotion |
| `sense(agent, radius, property)` | AB | Local perception |
| `decide(agent, rule)` | AB | State machine transition |
| `emit(event, time, target)` | DE | Schedule future event |
| `queue(entity, resource, priority)` | DE | Enter service queue |
| `serve(resource, duration)` | DE | Process entity |
| `route(entity, condition, dest)` | DE | Conditional branching |
| `stock(name, initial, bounds)` | SD | Declare accumulator |
| `flow(source, sink, rate_fn)` | SD | Continuous transfer |
| `feedback(stock, polarity, delay)` | SD | Causal loop |
| `diffuse(property, rate, radius)` | SD | Spatial diffusion |

---

## Sequence Index

| Seq | Cat | Theme | Patterns | Dominant Paradigm |
|-----|-----|-------|----------|-------------------|
| 01 | 1 | Regional Autonomy | 1 | SD |
| 02 | 1 | Settlement Distribution | 2–7 | SD ⊗ AB |
| 03 | 1 | Urban Mosaic | 8–11 | AB ⊗ SD |
| 04 | 1 | Community Scale | 12–15 | SD → AB |
| 05 | 1 | Transportation Networks | 16–20 | DE |
| 06 | 1 | Density & Sacred | 21–27 | SD ⊗ AB |
| 07 | 1 | Nucleus & Activity | 28–34 | AB ⊗ DE |
| 08 | 1 | Housing & Demographics | 35–40 | AB ⊗ SD |
| 09 | 1 | Work & Institutions | 41–50 | AB ⊗ DE |
| 10 | 1 | Paths & Streets | 51–57 | AB |
| 11 | 1 | Community Spaces | 58–64 | AB ⊗ DE |
| 12 | 1 | Life & Death | 65–71 | SD ⊗ AB |
| 13 | 1 | Family & Play | 72–78 | AB |
| 14 | 1 | Work & Commerce | 79–89 | AB ⊗ DE |
| 15 | 1 | Services & Rest | 90–94 | DE |
| 16 | 2 | Building Complex | 95–103 | AB ⊗ DE |
| 17 | 2 | Site & Form | 104–110 | AB |
| 18 | 2 | Envelope & Roof | 111–119 | AB |
| 19 | 2 | Circulation & Gradient | 120–131 | AB ⊗ DE |
| 20 | 2 | Rooms & Light | 132–142 | AB ⊗ DE |
| 21 | 2 | Sleep & Service | 143–151 | AB ⊗ DE |
| 22 | 2 | Work & Independence | 152–161 | AB |
| 23 | 2 | Outdoor Rooms | 162–171 | AB ⊗ SD |
| 24 | 2 | Gardens & Hearth | 172–182 | AB ⊗ SD |
| 25 | 2 | Workspace Layout | 183–189 | AB |
| 26 | 2 | Volume & View | 190–196 | AB |
| 27 | 2 | Volume & View (alt) | 190–196 | AB |
| 28 | 2 | Thick Walls & Storage | 197–204 | AB ⊗ DE |
| 29 | 3 | Structural Principle | 205–208 | DE |
| 30 | 3 | Roof & Floor Layout | 209–213 | DE |
| 31 | 3 | Foundation & Frame | 214–220 | DE |
| 32 | 3 | Openings | 221–225 | DE |
| 33 | 3 | Columns & Services | 226–232 | DE ⊗ SD |
| 34 | 3 | Surfaces & Finishes | 233–240 | DE |
| 35 | 3 | Outdoor Details | 241–248 | DE ⊗ SD |
| 36 | 3 | Ornament & Life | 249–253 | DE ⊗ AB |

---

## Complete Pattern Specification

### SEQUENCE 01 — Regional Autonomy (SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ01: The simulation world itself as an autonomous region                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL001 INDEPENDENT REGIONS**
```
NL: "Metropolitan regions will not come to balance until each one is small
     and autonomous enough to be an independent sphere of culture."

→ SD(
    stock("population", 5e6, [2e6, 10e6]),
    stock("autonomy", 0.5, [0, 1]),
    stock("cultural_intensity", 0.3, [0, 1]),
    flow("migration", external → population, ∂(economic_pull - push_factor)),
    flow("governance_decay", autonomy → void, ∂(population / 10e6)),
    feedback(autonomy, +, cultural_intensity),
    feedback(cultural_intensity, +, autonomy)
  )

Engine: world.config = { width, height, maxPopulation: 10e6 }
        sdIntegrator.addStock(population, autonomy, cultural_intensity)
```

---

### SEQUENCE 02 — Settlement Distribution (SD ⊗ AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ02: Power-law distribution of towns across landscape                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL002 DISTRIBUTION OF TOWNS**
```
NL: "Towns grow according to a power law: many small, few large."

→ SD ⊗ AB(
    stock("town_count", N, [1, 100]),
    flow("town_growth", void → town_count, ∂(migration_pressure)),
    spawn("settlement", pos=random_weighted(fertility), state={size: zipf(rank)}),
    feedback(town_count, -, land_availability)
  )
```

**APL003 CITY COUNTRY FINGERS**
```
NL: "Keep interlocking fingers of farmland and urban land."

→ AB ⊗ SD(
    σ(patch): if urban_neighbors > 4 AND rural_neighbors > 2 → type = "finger_edge",
    diffuse("urbanization", rate=0.01, radius=3),
    stock("urban_fraction", 0.3, [0.2, 0.6]),
    feedback(urban_fraction, -, agricultural_productivity)
  )
```

**APL004 AGRICULTURAL VALLEYS**
```
NL: "Preserve all agricultural valleys as farmland."

→ SD(
    stock("fertile_land", initial_area, [0, initial_area]),
    flow("development_pressure", fertile_land → built_land, ∂(population * 0.001)),
    feedback(fertile_land, +, food_security),
    constraint: if patch.elevation < valley_threshold → patch.protected = true
  )
```

**APL005 LACE OF COUNTRY STREETS**
```
NL: "Country roads should form a lace-like network."

→ AB(
    spawn("rural_road_builder", pos=village_center, state={energy: 30, heading: random}),
    σ(builder): move(heading + noise(5°), speed=0.5),
                if energy < 0 → die,
                patch.is_country_road = true
  )
```

**APL006 COUNTRY TOWNS**
```
NL: "Build small self-sufficient towns of 500-10000 in the countryside."

→ AB ⊗ SD(
    spawn("country_town", pos=intersection(country_roads), state={pop: uniform(500,10000)}),
    stock("self_sufficiency", 0.7, [0, 1]),
    flow("trade_dependency", self_sufficiency → void, ∂(distance_to_city / 100)),
    σ(town): if self_sufficiency < 0.5 → grow_local_industry()
  )
```

**APL007 THE COUNTRYSIDE**
```
NL: "Preserve countryside between settlements."

→ SD(
    stock("wilderness", initial_green, [0.4 * total, total]),
    flow("encroachment", wilderness → built, ∂(population_growth * sprawl_factor)),
    feedback(wilderness, +, quality_of_life),
    feedback(quality_of_life, -, migration_out)
  )
```

---

### SEQUENCE 03 — Urban Mosaic (AB ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ03: Cultural diversity and urban intensity                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL008 MOSAIC OF SUBCULTURES**
```
NL: "The city is a mosaic of subcultures, each with its own spatial territory."

→ AB(
    spawn("migrant", pos=entry_point, state={origin: pick(cultures), savings: 0}),
    σ(migrant): sense(radius=5, "cultural_affinity"),
               move_toward(max(affinity[own_culture])),
               patch.affinity[origin] += 0.1,
    diffuse("affinity_*", rate=0.01, radius=2),
    decay("affinity_*", rate=0.005)
  )
```

**APL009 SCATTERED WORK**
```
NL: "Decentralize workplaces throughout the community."

→ AB ⊗ DE(
    spawn("workshop", pos=within(residential_zone), state={capacity: uniform(3,12)}),
    σ(workshop): if demand > capacity → emit("expand", time+30, self),
    queue(worker, workshop, priority=distance),
    serve(workshop, duration=8h),
    constraint: max_distance(home, work) < 15min_walk
  )
```

**APL010 MAGIC OF THE CITY**
```
NL: "Concentrate urban density to create cultural intensity."

→ AB ⊗ SD(
    stock("cultural_density", 0, [0, 100]),
    flow("attraction", void → cultural_density, ∂(population_density * diversity_index)),
    σ(agent): if cultural_density(here) > threshold → stay_probability += 0.2,
    feedback(cultural_density, +, immigration_pull),
    feedback(cultural_density, -, cost_of_living)
  )
```

**APL011 LOCAL TRANSPORT AREAS**
```
NL: "Divide the city into transport areas of 1-2 miles diameter."

→ DE(
    emit("define_transport_zone", time=0, payload={radius: uniform(0.8, 1.6) miles}),
    route(vehicle, condition=crosses_boundary, dest=ring_road),
    queue(pedestrian, local_path, priority=0),
    constraint: internal_trips < 10min_walk
  )
```

---

### SEQUENCE 04 — Community Scale (SD → AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ04: Self-governing communities and their boundaries                       │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL012 COMMUNITY OF 7000**
```
NL: "Decentralize city government into communities of 5000-10000."

→ SD(
    stock("community_population", 0, [5000, 10000]),
    stock("governance_capacity", 1.0, [0, 1]),
    flow("growth", void → community_population, ∂(birth_rate + immigration)),
    flow("governance_strain", governance_capacity → void, ∂(population / 7000 - 1)),
    feedback(governance_capacity, -, community_population),
    trigger: if community_population > 10000 → emit("split_community")
  )
```

**APL013 SUBCULTURE BOUNDARY**
```
NL: "Encourage distinct boundaries between subcultures."

→ AB ⊗ SD(
    σ(patch): boundary_strength = |affinity[culture_A] - affinity[culture_B]|,
              if boundary_strength > 0.5 → type = "boundary_zone",
    stock("boundary_permeability", 0.5, [0, 1]),
    flow("hardening", boundary_permeability → void, ∂(conflict_index * 0.01)),
    spawn("boundary_marker", pos=max(boundary_strength), state={type: "park|market|wall"})
  )
```

**APL014 IDENTIFIABLE NEIGHBORHOOD**
```
NL: "Each neighborhood must be identifiable: <300 yards across, <500 people."

→ AB(
    σ(patch): density = count(agents, radius=150m),
              if density ∈ [50, 500] AND cluster_diameter < 300yd → neighborhood = true,
    spawn("neighborhood_marker", pos=centroid(cluster), state={name: generate_name()}),
    sense(agent, radius=150m, "neighborhood_id"),
    σ(agent): identity += 0.01 * (same_neighborhood_neighbors / total_neighbors)
  )
```

**APL015 NEIGHBORHOOD BOUNDARY**
```
NL: "Strengthen the boundary around each neighborhood."

→ AB ⊗ SD(
    σ(patch): if on_boundary(neighborhood) → land_use = "buffer",
    stock("boundary_integrity", 0.5, [0, 1]),
    flow("erosion", boundary_integrity → void, ∂(through_traffic * 0.01)),
    spawn("gateway", pos=road ∩ boundary, state={type: "arch|sign|narrowing"}),
    feedback(boundary_integrity, +, neighborhood_identity)
  )
```

---

### SEQUENCE 05 — Transportation Networks (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ05: Movement systems as discrete-event process flows                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL016 WEB OF PUBLIC TRANSPORTATION**
```
NL: "Establish a web of public transit connecting all communities."

→ DE(
    emit("bus_departure", time=schedule[i], payload={route, capacity: 40}),
    queue(passenger, bus_stop, priority=arrival_time),
    serve(bus, duration=route_time),
    route(passenger, condition=destination_reached, dest=alight),
    route(passenger, condition=transfer_needed, dest=interchange)
  )
```

**APL017 RING ROADS**
```
NL: "Place high-speed roads around transport areas, not through them."

→ AB ⊗ SD(
    spawn("ring_road_builder", pos=transport_area_boundary, state={heading: tangent}),
    σ(builder): follow_boundary(transport_area, offset=50m),
                patch.road_type = "arterial",
    stock("through_traffic", 0, [0, max_capacity]),
    flow("diversion", through_traffic → ring_traffic, ∂(ring_road_quality))
  )
```

**APL018 NETWORK OF LEARNING**
```
NL: "Distribute educational facilities throughout the community."

→ AB ⊗ DE(
    spawn("school", pos=neighborhood_center, state={capacity: 200, type: pick(levels)}),
    queue(student, school, priority=distance),
    serve(school, duration=term_length),
    σ(school): if utilization > 0.9 → emit("expand", time+365, self),
    constraint: max_walk_distance(home, school) < 1km
  )
```

**APL019 WEB OF SHOPPING**
```
NL: "Distribute shops in a web throughout the community."

→ DE(
    emit("customer_trip", time=daily_schedule, payload={needs: list, budget}),
    route(customer, condition=need_fulfilled, dest=next_shop ⊕ home),
    queue(customer, shop, priority=arrival),
    serve(shop, duration=transaction_time),
    constraint: shop_density ∝ 1/distance_to_center
  )
```

**APL020 MINI-BUSES**
```
NL: "Replace large buses with frequent mini-buses."

→ DE(
    emit("minibus_departure", time=every(5min), payload={capacity: 12, route: adaptive}),
    queue(passenger, nearest_stop, priority=wait_time),
    serve(minibus, duration=hop_time),
    route(minibus, condition=demand_signal, dest=deviation_point),
    σ(minibus): if passengers < 3 → skip_low_demand_stops
  )
```

---

### SEQUENCE 06 — Density & Sacred (SD ⊗ AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ06: Height limits, parking, sacred sites, water, life cycle               │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL021 FOUR-STORY LIMIT**
```
NL: "No building higher than four stories."

→ SD(
    constraint: building.height ≤ 4,
    stock("ground_connection", 1.0, [0, 1]),
    flow("alienation", ground_connection → void, ∂((height - 4) * 0.1)),
    feedback(ground_connection, +, community_engagement)
  )
```

**APL022 NINE PERCENT PARKING**
```
NL: "Do not allow more than 9% of any area to be used for parking."

→ SD(
    stock("parking_fraction", 0.05, [0, 0.09]),
    flow("demand_pressure", void → parking_fraction, ∂(car_ownership * 0.001)),
    constraint: parking_fraction ≤ 0.09,
    feedback(parking_fraction, -, walkability),
    feedback(walkability, -, car_dependency)
  )
```

**APL023 PARALLEL ROADS**
```
NL: "Separate pedestrian paths from vehicle roads."

→ AB(
    σ(patch): if is_road → spawn_parallel("pedestrian_path", offset=10m),
    σ(pedestrian): prefer(path.type == "pedestrian"),
    σ(vehicle): constrain(path.type == "vehicle"),
    sense(pedestrian, radius=2, "vehicle_proximity"),
    σ(pedestrian): if vehicle_proximity > 0 → comfort -= 0.1
  )
```

**APL024 SACRED SITES**
```
NL: "Preserve places that have come to symbolize the community."

→ AB(
    spawn("sacred_site", pos=historic_locations, state={age: centuries, significance: high}),
    σ(patch): if within(sacred_site, radius=50m) → protected = true, buildable = false,
    σ(agent): if near(sacred_site) → wellbeing += 0.05,
    constraint: sacred_site.neighbors.land_use ∉ {industrial, commercial_heavy}
  )
```

**APL025 ACCESS TO WATER**
```
NL: "Preserve public access to natural water bodies."

→ AB ⊗ SD(
    σ(patch): if elevation < water_table → type = "water_body",
    stock("water_access_quality", 0.5, [0, 1]),
    flow("pollution", water_access_quality → void, ∂(industrial_runoff)),
    spawn("waterfront_path", pos=water_edge, state={public: true}),
    σ(agent): if distance(water) < 500m → satisfaction += 0.1,
    feedback(water_access_quality, +, property_value)
  )
```

**APL026 LIFE CYCLE**
```
NL: "Provide housing for every stage of life."

→ SD(
    stock("youth_pop", 0, [0, ∞]),
    stock("adult_pop", 0, [0, ∞]),
    stock("elder_pop", 0, [0, ∞]),
    flow("aging_1", youth_pop → adult_pop, ∂(youth_pop / 18)),
    flow("aging_2", adult_pop → elder_pop, ∂(adult_pop / 45)),
    flow("death", elder_pop → void, ∂(elder_pop / 20)),
    flow("birth", void → youth_pop, ∂(adult_pop * fertility_rate)),
    feedback(elder_pop, +, wisdom_stock),
    feedback(youth_pop, +, vitality_stock)
  )
```

**APL027 MEN AND WOMEN**
```
NL: "Ensure equal access to community spaces for all genders."

→ AB(
    σ(agent): gender = pick(["M", "F", "NB"]),
    σ(space): accessibility_score = min(gender_counts) / max(gender_counts),
    constraint: accessibility_score > 0.7 for all public_spaces,
    σ(agent): if space.accessibility_score < 0.5 → avoid(space)
  )
```

---

### SEQUENCE 07 — Nucleus & Activity (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ07: Centers, density rings, activity nodes, commerce                       │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL028 ECCENTRIC NUCLEUS**
```
NL: "Locate the commercial core off-center from the geographic middle."

→ AB ⊗ SD(
    spawn("nucleus", pos=offset(center, random_direction, 0.2*radius)),
    stock("land_value", peak, [0, peak]),
    diffuse("land_value", rate=0.02, radius=30),
    flow("value_decay", land_value → void, ∂(distance_from_nucleus * 0.01)),
    σ(agent): attraction_to_nucleus = land_value(nucleus) / distance²
  )
```

**APL029 DENSITY RINGS**
```
NL: "Highest density at the center, decreasing outward in rings."

→ SD(
    stock("density[r]", peak * exp(-r/λ), [0, peak]),
    flow("densification", void → density[r], ∂(immigration * exp(-r/λ))),
    flow("sprawl", density[r] → density[r+1], ∂(congestion[r] * 0.01)),
    feedback(density[r], -, quality_of_life[r]),
    constraint: density[r] > density[r+1] ∀ r
  )
```

**APL030 ACTIVITY NODES**
```
NL: "Create activity nodes at the intersection of paths."

→ AB ⊗ DE(
    spawn("activity_node", pos=intersection(paths), state={intensity: 0}),
    σ(node): intensity = count(agents, radius=20) * path_count,
    emit("market_day", time=weekly, target=high_intensity_nodes),
    queue(vendor, node, priority=seniority),
    serve(node, duration=market_hours),
    σ(agent): if seeking_activity → move_toward(max(node.intensity))
  )
```

**APL031 PROMENADE**
```
NL: "Create a main pedestrian street for gathering and strolling."

→ AB(
    spawn("promenade", pos=connect(nucleus, activity_nodes[0..2])),
    σ(promenade_patch): width = 15m, vehicle_access = false,
    σ(agent): if leisure_time AND near(promenade) → enter_promenade,
              walk_speed *= 0.5, social_interaction_rate *= 3
  )
```

**APL032 SHOPPING STREET**
```
NL: "Line the promenade with shops."

→ DE(
    spawn("shop", pos=adjacent(promenade), state={type: pick(retail_types)}),
    queue(customer, shop, priority=need_urgency),
    serve(shop, duration=browse_time + transaction_time),
    emit("sale", time=now, payload={item, price, customer}),
    route(customer, condition=budget_exhausted, dest=home)
  )
```

**APL033 NIGHT LIFE**
```
NL: "Concentrate evening activities in specific areas."

→ DE(
    emit("evening_activation", time=18:00, target=nightlife_zone),
    spawn("venue", pos=nightlife_zone, state={type: pick(bar,club,theater), hours: [18,02]}),
    queue(patron, venue, priority=group_size),
    serve(venue, duration=exponential(2h)),
    σ(venue): if time ∉ hours → closed = true
  )
```

**APL034 INTERCHANGE**
```
NL: "Create comfortable transfer points between transit modes."

→ DE(
    spawn("interchange", pos=intersection(transit_routes)),
    queue(passenger, interchange, priority=connection_urgency),
    route(passenger, condition=next_mode_available, dest=boarding_point),
    emit("connection_missed", time=departure - arrival, condition=negative),
    serve(interchange, duration=wait_time + transfer_walk)
  )
```

---

### SEQUENCE 08 — Housing & Demographics (AB ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ08: Household diversity, clustering, hillside housing                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL035 HOUSEHOLD MIX**
```
NL: "Every neighborhood should contain a mix of household types."

→ AB ⊗ SD(
    σ(household): type = pick(["single", "couple", "family", "extended", "elderly"]),
    stock("diversity_index", 0, [0, 1]),
    flow("homogenization", diversity_index → void, ∂(price_sorting * 0.01)),
    σ(neighborhood): diversity = shannon_entropy(household_types),
    feedback(diversity_index, +, community_resilience)
  )
```

**APL036 DEGREES OF PUBLICNESS**
```
NL: "Create a gradient from public to private space."

→ AB(
    σ(patch): publicness = 1.0 - (distance_from_street / max_depth),
    σ(agent): comfort = match(agent.privacy_need, patch.publicness),
    sense(agent, radius=5, "publicness"),
    decide(agent, rule: move_toward(optimal_publicness))
  )
```

**APL037 HOUSE CLUSTER**
```
NL: "Group 8-12 houses around shared common land."

→ AB(
    spawn("house_cluster", pos=neighborhood_interior, state={houses: 0, max: 12}),
    σ(cluster): if houses < max → spawn("house", pos=ring(center, 20m)),
    spawn("common_land", pos=cluster.center, state={shared: true, area: 500m²}),
    σ(resident): if on_common_land → social_interaction_rate *= 2
  )
```

**APL038 ROW HOUSES**
```
NL: "For medium density, build row houses with private gardens."

→ AB(
    spawn("row_house", pos=along(street), state={width: 6m, depth: 12m, garden: rear}),
    σ(row): spacing = 0, orientation = street_facing,
    constraint: density ∈ [30, 60] units/hectare,
    σ(resident): garden_access = true, street_presence = true
  )
```

**APL039 HOUSING HILL**
```
NL: "On hillsides, step buildings to preserve views and sun."

→ AB ⊗ SD(
    σ(patch): if slope > 15° → building_type = "stepped",
    σ(building): height ≤ uphill_neighbor.ground_level,
    stock("view_quality", 1.0, [0, 1]),
    flow("obstruction", view_quality → void, ∂(downhill_building_height * 0.1)),
    constraint: each_unit.solar_access ≥ 4h/day
  )
```

**APL040 OLD PEOPLE EVERYWHERE**
```
NL: "Integrate elderly throughout the community, not in ghettos."

→ AB(
    σ(elder_agent): type = "elderly", mobility = reduced, wisdom = high,
    constraint: elder_housing.distribution = uniform(neighborhoods),
    σ(elder): if isolated → wellbeing -= 0.1/tick,
              if near(children) → wellbeing += 0.05,
    σ(neighborhood): elder_fraction ∈ [0.1, 0.3]
  )
```

---

### SEQUENCE 09 — Work & Institutions (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ09: Workplaces, industry, education, governance, markets                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL041 WORK COMMUNITY**
```
NL: "Create self-contained workplace clusters with their own character."

→ AB ⊗ DE(
    spawn("work_community", pos=periphery, state={industry: type, workers: 0, capacity: 200}),
    queue(worker, work_community, priority=skill_match),
    serve(work_community, duration=shift_length),
    σ(work_community): character = dominant_culture(workers),
    emit("shift_change", time=every(8h), target=work_community)
  )
```

**APL042 INDUSTRIAL RIBBON**
```
NL: "Place industry in ribbons along rail and major roads."

→ AB ⊗ SD(
    σ(patch): if adjacent(rail ∪ arterial) AND land_value < median → industrial_candidate,
    spawn("factory", pos=industrial_candidate, state={pollution: type, jobs: N}),
    stock("industrial_output", 0, [0, max]),
    flow("production", raw_materials → industrial_output, ∂(workers * productivity)),
    diffuse("pollution", rate=0.05, radius=20),
    feedback(pollution, -, residential_value)
  )
```

**APL043 UNIVERSITY AS MARKETPLACE**
```
NL: "Make the university an open marketplace of learning."

→ AB ⊗ DE(
    spawn("university", pos=community_center, state={open: true, departments: N}),
    σ(university): boundary = permeable, public_access = true,
    queue(learner, department, priority=interest_level),
    serve(department, duration=course_length),
    emit("public_lecture", time=weekly, target=community)
  )
```

**APL044 LOCAL TOWN HALL**
```
NL: "Give each community its own town hall for self-governance."

→ AB(
    spawn("town_hall", pos=community_center, state={capacity: 200, functions: [governance, meeting]}),
    σ(citizen): if grievance > threshold → visit(town_hall),
    σ(town_hall): decisions = vote(present_citizens),
    emit("community_meeting", time=monthly, target=all_citizens)
  )
```

**APL045 NECKLACE OF COMMUNITY PROJECTS**
```
NL: "Ring the community with a necklace of projects and facilities."

→ AB ⊗ DE(
    spawn("community_project", pos=ring(community_center, 200m), 
          state={type: pick(library, clinic, workshop, garden)}),
    emit("project_activation", time=opening_hours, target=project),
    queue(citizen, project, priority=need),
    serve(project, duration=visit_time),
    σ(project): if utilization < 0.3 → emit("repurpose", time+180d)
  )
```

**APL046 MARKET OF MANY SHOPS**
```
NL: "Create markets with many small shops rather than few large ones."

→ DE(
    spawn("market", pos=activity_node, state={stalls: 30, type: "covered"}),
    spawn("stall", pos=within(market), state={goods: type, owner: individual}),
    queue(customer, stall, priority=proximity),
    serve(stall, duration=haggle_time),
    emit("market_day", time=daily(6:00-14:00), target=market),
    constraint: stall.size ≤ 20m²
  )
```

**APL047 HEALTH CENTER**
```
NL: "Place a health center in each community."

→ DE(
    spawn("health_center", pos=community_center, state={beds: 20, staff: 10}),
    queue(patient, health_center, priority=severity),
    serve(health_center, duration=treatment_time),
    route(patient, condition=severity > critical, dest=hospital),
    emit("wellness_check", time=quarterly, target=elderly_agents)
  )
```

**APL048 HOUSING IN BETWEEN**
```
NL: "Mix housing among workplaces."

→ AB(
    σ(patch): if work_zone AND residential_fraction < 0.3 → allow_housing,
    spawn("live_work_unit", pos=work_zone_edge, state={floors: [work, live]}),
    σ(resident): commute_time = 0, work_life_integration = high,
    constraint: noise_level(residential_patch) < 55dB
  )
```

**APL049 LOOPED LOCAL ROADS**
```
NL: "Make local roads loop and branch, never grid."

→ AB(
    spawn("road_builder", pos=service_center, state={heading: random, energy: 50, order: 1}),
    σ(builder): move(heading + noise(σ=5°), speed=1),
                if energy > 20 AND tick%15==0 → hatch("road_builder", heading±90°, energy*0.5),
                patch.is_road = true,
                if energy ≤ 0 → die,
    constraint: no_grid_pattern, no_through_traffic
  )
```

**APL050 T JUNCTIONS**
```
NL: "Use T-junctions instead of crossroads to slow traffic."

→ AB(
    σ(road_builder): if intersecting(other_road) → terminate(offset=1),
    σ(junction): type = "T", through_speed = reduced,
    σ(vehicle): if at(T_junction) → decelerate, yield,
    constraint: crossroad_count = 0 within local_roads
  )
```

---

### SEQUENCE 10 — Paths & Streets (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ10: Pedestrian infrastructure and children's safety                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL051 GREEN STREETS**
```
NL: "Line residential streets with trees and grass."

→ AB ⊗ SD(
    σ(patch): if is_road AND residential_zone → spawn("street_tree", pos=edge),
    stock("canopy_cover", 0, [0, 0.8]),
    flow("growth", void → canopy_cover, ∂(tree_age * 0.01)),
    σ(street_tree): shade_radius = age * 0.5, cooling_effect = shade_radius * 0.1,
    feedback(canopy_cover, +, walkability)
  )
```

**APL052 NETWORK OF PATHS AND CARS**
```
NL: "Create two separate networks: one for cars, one for pedestrians."

→ AB ⊗ DE(
    σ(patch): network_type = "vehicle" ⊕ "pedestrian" ⊕ "shared",
    σ(pedestrian): route = shortest_path(pedestrian_network),
    σ(vehicle): route = shortest_path(vehicle_network),
    queue(pedestrian, crossing, priority=wait_time),
    emit("crossing_signal", time=every(60s), target=crossing),
    constraint: pedestrian_network.coverage ≥ vehicle_network.coverage
  )
```

**APL053 MAIN GATEWAYS**
```
NL: "Mark the main entries to each neighborhood with gateways."

→ AB(
    spawn("gateway", pos=road ∩ neighborhood_boundary, state={type: "arch|sign|narrowing"}),
    σ(gateway): visible_from = 50m, speed_reduction = 0.3,
    σ(vehicle): if passing(gateway) → speed *= (1 - speed_reduction),
    σ(pedestrian): if passing(gateway) → sense_of_arrival += 0.5
  )
```

**APL054 ROAD CROSSING**
```
NL: "Raise pedestrian crossings above road level."

→ DE(
    spawn("crossing", pos=intersection(pedestrian_path, vehicle_road)),
    queue(pedestrian, crossing, priority=wait_time),
    emit("crossing_phase", time=every(90s), target=crossing),
    serve(crossing, duration=15s),
    σ(crossing): type = "raised" → vehicle_speed_limit = 20kph
  )
```

**APL055 RAISED WALK**
```
NL: "Raise pedestrian paths above vehicle roads."

→ AB(
    σ(patch): if pedestrian_path AND adjacent(vehicle_road) → elevation += 0.3m,
    σ(pedestrian): if on_raised_walk → safety = high, visibility = enhanced,
    σ(vehicle): cannot_access(raised_walk),
    constraint: raised_walk.width ≥ 3m
  )
```

**APL056 BIKE PATHS AND RACKS**
```
NL: "Create a network of bike paths with racks at destinations."

→ AB ⊗ DE(
    spawn("bike_path", pos=parallel(main_roads, offset=5m)),
    spawn("bike_rack", pos=activity_nodes, state={capacity: 20}),
    σ(cyclist): route = shortest_path(bike_network),
    queue(cyclist, bike_rack, priority=proximity),
    constraint: bike_path.separation_from_cars ≥ 2m
  )
```

**APL057 CHILDREN IN THE CITY**
```
NL: "Design cities so children can move safely and independently."

→ AB(
    σ(child_agent): age ∈ [5, 15], range = age * 50m, independence = age / 15,
    σ(patch): child_safety = 1 - vehicle_traffic * 0.1,
    sense(child, radius=range, "child_safety"),
    σ(child): if child_safety(here) < 0.5 → retreat_home,
    constraint: path_to_school.child_safety > 0.8 ∀ patches
  )
```

---

### SEQUENCE 11 — Community Spaces (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ11: Celebrations, parks, squares, water features                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL058 CARNIVAL**
```
NL: "Periodic community celebrations in public spaces."

→ DE(
    emit("carnival", time=seasonal, target=public_square),
    spawn("carnival_event", pos=square, state={duration: 3d, attractions: N}),
    queue(citizen, attraction, priority=interest),
    serve(attraction, duration=exponential(30min)),
    σ(citizen): if attending_carnival → happiness += 0.3, social_bonds += 0.1
  )
```

**APL059 QUIET BACKS**
```
NL: "Behind busy areas, create quiet, peaceful zones."

→ AB(
    σ(patch): if behind(commercial_front) AND distance(road) > 20m → quiet_back = true,
    σ(quiet_back): noise_level = ambient * 0.2, vehicle_access = false,
    σ(agent): if stress > threshold → seek(quiet_back),
              if in(quiet_back) → stress -= 0.1/tick
  )
```

**APL060 ACCESSIBLE GREEN**
```
NL: "Everyone should be within 3 minutes walk of a green space."

→ AB ⊗ SD(
    constraint: ∀ patch: min_distance(green_space) < 200m,
    spawn("green_space", pos=gap_in_coverage, state={area: ≥ 500m², type: park}),
    stock("green_coverage", 0, [0, 1]),
    flow("development_loss", green_coverage → void, ∂(building_pressure * 0.001)),
    σ(agent): if distance(green) < 200m → health += 0.01/tick,
    feedback(green_coverage, +, property_value)
  )
```

**APL061 SMALL PUBLIC SQUARES**
```
NL: "Create small squares (15-20m across) at path intersections."

→ AB(
    spawn("small_square", pos=intersection(paths), state={diameter: uniform(15,22)m}),
    σ(square): seating = perimeter * 0.3, enclosure = 0.7,
    σ(agent): if passing(square) AND leisure → stop_probability = 0.3,
              if stopped → social_interaction_rate *= 2
  )
```

**APL062 HIGH PLACES**
```
NL: "Create elevated viewpoints accessible to all."

→ AB(
    spawn("high_place", pos=max(elevation, within=neighborhood), state={public: true}),
    σ(agent): if seeking_perspective → move_toward(high_place),
              if at(high_place) → view_radius = 500m, contemplation += 0.1
  )
```

**APL063 DANCING IN THE STREET**
```
NL: "Create spaces for spontaneous music and dance."

→ DE(
    spawn("performance_space", pos=widening(pedestrian_path), state={acoustics: good}),
    emit("spontaneous_performance", time=random(evening), probability=0.1),
    queue(audience, performance_space, priority=proximity),
    serve(performance_space, duration=exponential(45min)),
    σ(agent): if witnessing_performance → joy += 0.2
  )
```

**APL064 POOLS AND STREAMS**
```
NL: "Bring natural water features into the community."

→ AB ⊗ SD(
    σ(patch): if elevation < water_table + 1m → potential_stream = true,
    spawn("stream", pos=connect(water_sources, downhill)),
    stock("water_flow", seasonal_avg, [0, flood_level]),
    flow("rainfall", void → water_flow, ∂(precipitation)),
    flow("evaporation", water_flow → void, ∂(temperature * 0.01)),
    σ(agent): if near(stream) → tranquility += 0.05
  )
```

---

### SEQUENCE 12 — Life & Death (SD ⊗ AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ12: Birth, death, sacred ground, common land                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL065 BIRTH PLACES**
```
NL: "Create local birthing facilities in each community."

→ DE(
    spawn("birth_center", pos=community, state={midwives: 3, beds: 5}),
    emit("birth_event", time=stochastic(birth_rate), target=birth_center),
    queue(mother, birth_center, priority=urgency),
    serve(birth_center, duration=labor_time),
    route(mother, condition=complications, dest=hospital)
  )
```

**APL066 HOLY GROUND**
```
NL: "Set aside sacred ground for contemplation and memorial."

→ AB(
    spawn("holy_ground", pos=quiet_elevated_site, state={age: ancient, use: memorial}),
    σ(holy_ground): silence_radius = 30m, no_construction = true,
    σ(agent): if grieving OR contemplating → seek(holy_ground),
              if at(holy_ground) → peace += 0.1/tick
  )
```

**APL067 COMMON LAND**
```
NL: "Provide common land shared by house clusters."

→ AB ⊗ SD(
    spawn("common_land", pos=center(house_cluster), state={area: 1000m², shared: true}),
    stock("soil_fertility", 0.7, [0, 1]),
    flow("cultivation", void → soil_fertility, ∂(gardening_activity * 0.01)),
    flow("depletion", soil_fertility → void, ∂(overuse * 0.005)),
    σ(resident): if on(common_land) → community_bonds += 0.02
  )
```

**APL068 CONNECTED PLAY**
```
NL: "Link children's play areas so they can roam safely."

→ AB(
    spawn("play_area", pos=between(house_clusters), state={type: pick(adventure,sandbox,field)}),
    σ(play_area): connected_to = nearest(other_play_areas, max_distance=200m),
    spawn("play_path", pos=connect(play_areas), state={child_safe: true, vehicle_free: true}),
    σ(child): if playing → roam(play_path_network, range=age*30m)
  )
```

**APL069 PUBLIC OUTDOOR ROOM**
```
NL: "Create outdoor spaces enclosed enough to feel like rooms."

→ AB(
    spawn("outdoor_room", pos=courtyard_site, state={enclosure: 0.7, ceiling: sky/trellis}),
    σ(outdoor_room): walls = buildings + hedges + walls, floor = paved + planted,
    σ(agent): if in(outdoor_room) → comfort = indoor_level, social_ease = high
  )
```

**APL070 GRAVE SITES**
```
NL: "Integrate burial grounds into the community fabric."

→ AB(
    spawn("grave_site", pos=within(neighborhood), state={capacity: 200, age: 0}),
    σ(grave_site): visibility = "from_daily_paths", maintenance = community,
    σ(agent): if passing(grave_site) → mortality_awareness += 0.01, gratitude += 0.01
  )
```

**APL071 STILL WATER**
```
NL: "Create still, reflective pools in public spaces."

→ AB ⊗ SD(
    spawn("still_pool", pos=public_space, state={depth: 0.3m, area: 10m²}),
    stock("water_clarity", 1.0, [0, 1]),
    flow("algae", water_clarity → void, ∂(temperature * sunlight * 0.001)),
    flow("filtration", void → water_clarity, ∂(filter_capacity)),
    σ(agent): if near(still_pool) → contemplation += 0.05, stress -= 0.02
  )
```

---

### SEQUENCE 13 — Family & Play (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ13: Household formation, children, and play territories                 │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL072 LOCAL SPORTS**
```
NL: "Scatter small sports places through the community so play is part of daily life."

→ AB ⊗ DE(
    spawn("sports_ground", pos=scattered(neighborhoods), state={game: pick(court, field, gym, pool), visible: true}),
    σ(sports_ground): frontage = open(to_street), spectators = welcome,
    queue(player, sports_ground, priority=arrival),
    serve(sports_ground, duration=game_time),
    σ(agent): if passing(sports_ground) → exercise_prompt += 0.05
  )
```

**APL073 ADVENTURE PLAYGROUND**
```
NL: "Give children raw materials and loose parts to build their own play worlds."

→ AB(
    spawn("adventure_playground", pos=edge(common_land), state={materials: [planks, tires, rope, earth], fixed_equipment: none}),
    σ(child): if at(adventure_playground) → build(structure_of_own_design),
              creativity += 0.1/session, risk_skill += 0.05,
    σ(playground): form = emergent(child_constructions), adult_control = minimal
  )
```

**APL074 ANIMALS**
```
NL: "Let domestic animals live within the neighborhood, not fenced out of daily life."

→ AB ⊗ SD(
    spawn("animal", pos=neighborhood, state={species: pick(horse, chicken, goat, duck), free_range: bounded}),
    σ(path): surface = tolerant(hooves), traffic = slow,
    stock("animal_population", N0, [0, carrying_capacity]),
    flow("births", void → animal_population, ∂(fertility * population)),
    σ(child): if near(animal) → care_skill += 0.02, empathy += 0.02
  )
```

**APL075 THE FAMILY**
```
NL: "House extended families so generations can support one another."

→ AB(
    spawn("family_compound", pos=lot, state={units: [elder, couple, single], commons: shared_kitchen ∪ garden}),
    σ(family): membership = multi_generational, mutual_aid = high,
    σ(member): private_realm = own_unit, shared_realm = commons,
    σ(compound): if member.crisis → support_response = internal_first
  )
```

**APL076 HOUSE FOR A SMALL FAMILY**
```
NL: "Organize the small family house around a common realm with distinct private wings."

→ AB(
    spawn("house_small_family", pos=lot, state={realms: [couple, children, commons]}),
    σ(house): commons = largest(hearth, kitchen, eating),
    σ(couple_realm): separated(acoustic, visual) from children_realm,
    σ(child_realm): opens_to(outdoor_play), scaled_to(child)
  )
```

**APL077 HOUSE FOR A COUPLE**
```
NL: "Shape the couple's house as two overlapping worlds with room for each person."

→ AB(
    spawn("house_couple", pos=lot, state={realms: [shared, hers, his]}),
    σ(house): shared_realm = generous(kitchen, sitting, garden),
    σ(partner): own_room = small_but_inviolate,
    σ(couple): if both_home → co_presence += 0.05, autonomy = preserved
  )
```

**APL078 HOUSE FOR ONE PERSON**
```
NL: "Make the one-person house a compact shell that can grow."

→ AB(
    spawn("house_single", pos=infill ∪ cottage_site, state={area: ≤ 40m², alcoves: [bed, cook, work]}),
    σ(house): center = one_room(living), edges = alcoved(functions),
    σ(occupant): control = total(personalization),
    σ(house): expandable = true(when_household_grows)
  )
```

---

### SEQUENCE 14 — Work & Commerce (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ14: Workshops, commerce, and neighborhood-scale exchange                │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL079 YOUR OWN HOME**
```
NL: "Guarantee every household a dwelling it owns and can shape."

→ AB ⊗ SD(
    σ(household): tenure = owned ∨ secured(long_term),
    spawn("dwelling", pos=lot, state={owner: household, mutable: true}),
    stock("home_investment", 0, [0, max]),
    flow("improvement", income → home_investment, ∂(ownership * care_rate)),
    feedback(home_investment, +, neighborhood_quality, delay=5y)
  )
```

**APL080 SELF-GOVERNING WORKSHOPS AND OFFICES**
```
NL: "Let workgroups own and govern their own workplaces."

→ AB ⊗ DE(
    spawn("workshop", pos=work_community, state={owners: workers, size: ≤ 20}),
    σ(workshop): decisions = consensus(workers), profit = shared,
    queue(order, workshop, priority=FIFO),
    serve(workshop, duration=craft_time(order)),
    σ(worker): autonomy += 0.1, alienation -= 0.1
  )
```

**APL081 SMALL SERVICES WITHOUT RED TAPE**
```
NL: "Break services into small direct units without bureaucratic layers."

→ DE(
    spawn("service_point", pos=distributed(community), state={staff: ≤ 12, autonomy: full}),
    queue(client, service_point, priority=arrival),
    serve(service_point, duration=direct_service_time),
    route(client, condition=needs_specialist, dest=peer_service_point),
    constraint: intermediaries = 0
  )
```

**APL082 OFFICE CONNECTIONS**
```
NL: "Place groups that work together within easy walking reach of each other."

→ AB(
    σ(office_group): ∀ g2 ∈ collaborators(g1): distance(g1, g2) ≤ f(interaction_rate),
    decide(planner, rule=minimize Σ interaction_rate * distance),
    σ(worker): if trip(g1 → g2) > 3min_walk → interaction_rate -= 0.2
  )
```

**APL083 MASTER AND APPRENTICES**
```
NL: "Arrange work so newcomers learn beside experienced masters."

→ AB ⊗ DE(
    spawn("atelier", pos=workshop, state={master: 1, apprentices: ≤ 6}),
    queue(apprentice, atelier, priority=commitment),
    serve(atelier, duration=learning_years),
    σ(apprentice): skill += ∂(observation * practice),
    route(apprentice, condition=skill ≥ mastery, dest=new_atelier(as_master))
  )
```

**APL084 TEENAGE SOCIETY**
```
NL: "Give teenagers a semi-autonomous society bridging home and adult work."

→ AB ⊗ DE(
    spawn("teen_society", pos=own_premises(community), state={governance: teen_led, adult_advisors: few}),
    σ(teenager): membership = voluntary, responsibility = real(projects, budget),
    emit("apprentice_placement", time=each_term, target=work_community),
    queue(teen, placement, priority=interest),
    serve(placement, duration=term_length)
  )
```

**APL085 SHOPFRONT SCHOOLS**
```
NL: "Run small schools in shopfronts along active streets, not in isolated campuses."

→ AB ⊗ DE(
    spawn("shopfront_school", pos=fronting(pedestrian_street), state={pupils: ≤ 20, teachers: 2}),
    σ(school): window = open_to(street_life), curriculum = town_as_classroom,
    emit("field_walk", time=daily, target=surrounding_blocks),
    queue(pupil, school, priority=proximity),
    serve(school, duration=school_day)
  )
```

**APL086 CHILDREN'S HOME**
```
NL: "Provide a home-like place where children can stay and be cared for at any hour."

→ AB ⊗ DE(
    spawn("childrens_home", pos=adjacent(community_center), state={beds: 8, open: 24h}),
    queue(child, childrens_home, priority=need),
    serve(childrens_home, duration=stay_length),
    σ(childrens_home): atmosphere = domestic(not_institutional),
    σ(parent): trust(childrens_home) → labor_flexibility += 0.1
  )
```

**APL087 INDIVIDUALLY OWNED SHOPS**
```
NL: "Favor shops owned by the people who run them."

→ AB ⊗ DE(
    spawn("shop", pos=market ∪ street, state={owner: operator, chain: false}),
    σ(shop): character = personal(owner_taste), stock = local_selection,
    queue(customer, shop, priority=arrival),
    serve(shop, duration=personal_service_time),
    constraint: ownership = individual, franchise → discouraged(rent_policy)
  )
```

**APL088 STREET CAFE**
```
NL: "Open cafes onto the street where people can sit, watch, and linger."

→ AB ⊗ DE(
    spawn("street_cafe", pos=edge(public_path), state={terrace: sidewalk, tables: small}),
    queue(patron, street_cafe, priority=arrival),
    serve(street_cafe, duration=lingering_time(unhurried)),
    σ(patron): people_watching += 0.1, public_life += 0.05,
    σ(cafe): boundary = permeable(street ↔ terrace ↔ interior)
  )
```

**APL089 CORNER GROCERY**
```
NL: "Keep a small grocery within walking distance of every home."

→ AB ⊗ DE(
    spawn("corner_grocery", pos=corner(residential_block), state={goods: daily_needs}),
    constraint: ∀ home: distance(home, nearest_grocery) ≤ 300m,
    queue(shopper, corner_grocery, priority=arrival),
    serve(corner_grocery, duration=quick_trip),
    σ(grocer): knows(regulars) → neighborhood_watch += 0.05
  )
```

---

### SEQUENCE 15 — Services & Rest (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ15: Inns, stops, food, and rest as service processes                    │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL090 BEER HALL**
```
NL: "Provide a big convivial hall where strangers and friends drink and sing together."

→ DE(
    spawn("beer_hall", pos=community_crossroads, state={tables: long_shared, capacity: 150}),
    emit("evening_opening", time=daily(18:00-24:00), target=beer_hall),
    queue(patron, shared_table, priority=arrival),
    serve(beer_hall, duration=social_evening),
    σ(patron): mixing = cross_group(strangers ↔ friends), song_probability = f(crowd_density)
  )
```

**APL091 TRAVELER'S INN**
```
NL: "Offer travelers a communal inn where they eat and sleep among others."

→ DE(
    spawn("travelers_inn", pos=edge(town, on_road), state={hearth: common, rooms: around_court}),
    emit("traveler_arrival", time=poisson(road_flow), target=travelers_inn),
    queue(traveler, travelers_inn, priority=arrival),
    serve(travelers_inn, duration=overnight),
    σ(traveler): stories_exchanged += 1, isolation -= 0.1
  )
```

**APL092 BUS STOP**
```
NL: "Make each bus stop a small useful place, not a bare pole."

→ DE(
    spawn("bus_stop", pos=path_junction, state={shelter: true, adjacent: kiosk ∪ bench ∪ tree}),
    emit("bus_arrival", time=every(headway), target=bus_stop),
    queue(rider, bus_stop, priority=arrival),
    serve(boarding, duration=dwell_time),
    σ(waiting_rider): if useful_adjacency → wait_burden -= 0.5
  )
```

**APL093 FOOD STANDS**
```
NL: "Let food stands cluster where people naturally pass and gather."

→ DE(
    spawn("food_stand", pos=flow_concentration(paths), state={menu: few_items, front: counter}),
    emit("meal_peak", time=daily([12:00, 18:00]), target=food_stand),
    queue(customer, food_stand, priority=arrival),
    serve(food_stand, duration=short_prep),
    σ(street): food_aroma → dwell_time += 0.1
  )
```

**APL094 SLEEPING IN PUBLIC**
```
NL: "Accept dozing in public as a sign of a safe, relaxed commons."

→ AB(
    σ(public_space): if safety > 0.7 AND seats = comfortable → napping = acceptable,
    σ(agent): if fatigue > 0.8 AND in(safe_public_space) → rest(bench ∪ lawn),
    σ(observer): reaction = tolerant, alarm = false,
    sense(agent, radius=10m, property=calm) → fatigue -= 0.2
  )
```

---

### SEQUENCE 16 — Building Complex (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ16: Buildings grouped into coherent complexes                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL095 BUILDING COMPLEX**
```
NL: "Compose large programs as families of smaller connected buildings."

→ AB(
    spawn("building_complex", pos=site, state={parts: many_small, connections: paths ∪ bridges}),
    constraint: single_monolithic_mass = false,
    σ(part): size ≤ human_scale(4_stories), identity = legible,
    σ(complex): coherence = shared_language(materials, roofs) * connectivity(parts)
  )
```

**APL096 NUMBER OF STORIES**
```
NL: "Keep buildings at four stories or fewer."

→ AB ⊗ SD(
    constraint: building.stories ≤ 4,
    stock("ground_contact", 1.0, [0, 1]),
    flow("detachment", ground_contact → void, ∂(max(0, stories - 4) * 0.1)),
    σ(resident): if floor ≤ 4 → street_connection = maintained,
    σ(district): height_mix = varied(1..4)
  )
```

**APL097 SHIELDED PARKING**
```
NL: "Hide necessary parking behind or beneath inhabited edges."

→ AB ⊗ DE(
    spawn("parking_structure", pos=interior(block) ∪ below_grade, state={shield: inhabited_frontage}),
    constraint: parked_cars.visible_from(public_path) = false,
    queue(car, parking_structure, priority=arrival),
    serve(parking_structure, duration=stay),
    route(driver, condition=parked, dest=pedestrian_exit(on_active_street))
  )
```

**APL098 CIRCULATION REALMS**
```
NL: "Structure movement as a nested hierarchy of clearly named realms."

→ AB ⊗ DE(
    σ(complex): realms = nested(entry → court → wing → room),
    σ(realm): gateway = marked, name = displayed,
    route(visitor, condition=destination.realm, dest=next_gateway(realm_path)),
    σ(visitor): orientation = f(realm_legibility), lost_probability → 0
  )
```

**APL099 MAIN BUILDING**
```
NL: "Give every complex one main building as its visible heart."

→ AB(
    spawn("main_building", pos=focus(complex), state={height: taller, entrance: prominent}),
    σ(main_building): houses(core_function), visible_from(all_approaches),
    σ(other_building): defers_to(main_building),
    σ(visitor): first_reading = main_building → orientation += 0.2
  )
```

**APL100 PEDESTRIAN STREET**
```
NL: "Connect buildings with outdoor pedestrian streets, not interior corridors."

→ AB ⊗ DE(
    spawn("pedestrian_street", pos=between(buildings), state={width: 3-6m, vehicles: none}),
    σ(building): doors_open_onto(pedestrian_street),
    route(occupant, condition=inter_building_trip, dest=pedestrian_street),
    σ(street): if doorway_density > 1/15m → life = continuous
  )
```

**APL101 BUILDING THOROUGHFARE**
```
NL: "Where a route must pass through a building, make it a lively public passage."

→ AB ⊗ DE(
    spawn("thoroughfare", pos=through(ground_floor), state={width: generous, daylight: both_ends}),
    σ(thoroughfare): lined_with(shops ∪ workshops ∪ windows),
    route(pedestrian, condition=shortcut_available, dest=thoroughfare),
    serve(passage, duration=stroll_time),
    σ(building): interior_life += street_flow * 0.1
  )
```

**APL102 FAMILY OF ENTRANCES**
```
NL: "Shape all entrances in a complex as a recognizable family."

→ AB(
    σ(entrance_set): shared_form = {roof, recess, trim} with local_variation,
    constraint: ∀ e ∈ complex.entrances: similarity(e, family_prototype) ≥ 0.7,
    σ(entrance): individual_identity = variation(color, ornament),
    σ(visitor): entrance_recognition = immediate(family_resemblance)
  )
```

**APL103 SMALL PARKING LOTS**
```
NL: "Split parking into small lots screened by walls and planting."

→ AB ⊗ DE(
    spawn("parking_lot", pos=distributed(site), state={spaces: ≤ 7, screen: hedge ∪ wall}),
    constraint: lot.spaces ≤ 7, inter_lot_distance ≥ 30m,
    queue(car, nearest_lot, priority=availability),
    serve(parking_lot, duration=stay),
    σ(pedestrian): perceived_car_dominance -= f(lot_size_reduction)
  )
```

---

### SEQUENCE 17 — Site & Form (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ17: Site repair, orientation, and basic building form                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL104 SITE REPAIR**
```
NL: "Build on the damaged parts of a site and leave the beautiful parts untouched."

→ AB(
    σ(patch): quality = f(vegetation, views, microclimate),
    decide(builder, rule=place(building, argmin(patch.quality))),
    σ(best_patches): preserved = true, program = outdoor_life,
    σ(site): total_quality' > total_quality (repair, not consumption)
  )
```

**APL105 SOUTH FACING OUTDOORS**
```
NL: "Open the main outdoor spaces to the south of their buildings."

→ AB(
    σ(building): position = north_edge(lot),
    σ(outdoor_space): aspect = south(sun_most_of_day),
    constraint: shadow(building, outdoor_space, at=noon) ≈ 0,
    σ(occupant): outdoor_use = f(sunlight_hours)
  )
```

**APL106 POSITIVE OUTDOOR SPACE**
```
NL: "Shape outdoor space as positive enclosed figure, never leftover gaps."

→ AB(
    σ(outdoor_space): figure = convex(partial_enclosure 0.5-0.8),
    constraint: leftover_space = none(every_outdoor_area_has_shape_and_use),
    σ(building_edge): forms(walls_of_outdoor_rooms),
    σ(agent): if in(positive_space) → dwell_probability += 0.2
  )
```

**APL107 WINGS OF LIGHT**
```
NL: "Build in narrow wings so every room has natural light."

→ AB(
    constraint: wing.depth ≤ 7.5m,
    σ(building): form = wings(articulated, not_block),
    σ(room): daylight = f(distance_to_window⁻¹) > threshold,
    σ(wing): length = free, junction = shared_corners
  )
```

**APL108 CONNECTED BUILDINGS**
```
NL: "Join buildings to their neighbors instead of leaving useless slivers."

→ AB(
    σ(building): party_walls = shared(where_possible),
    constraint: gap(building, neighbor) ∈ {0} ∪ [usable_width, max],
    σ(urban_fabric): continuity += connection_rate,
    σ(sliver_space): eliminated → maintenance_burden -= 0.1
  )
```

**APL109 LONG THIN HOUSE**
```
NL: "Prefer a long thin plan that stretches distance between household realms."

→ AB(
    spawn("long_thin_house", pos=lot, state={depth: ≤ 6m, length: extended}),
    σ(house): rooms = chained(along_length), light = both_sides,
    σ(member): privacy = f(distance_along_house),
    σ(household): friction = decreases_with(separation_of_realms)
  )
```

**APL110 MAIN ENTRANCE**
```
NL: "Place the main entrance where it is seen at once from the approach."

→ AB(
    σ(main_entrance): position = intersect(approach_sightlines), form = bold(relief, roof, depth),
    constraint: visible_from(approach_path, ≥ 50m),
    σ(visitor): path = direct(main_entrance), search_time → 0,
    σ(visitor): arrival_confidence += 0.2
  )
```

---

### SEQUENCE 18 — Envelope & Roof (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ18: Roof form, sheltering edges, and transition to enclosure            │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL111 HALF-HIDDEN GARDEN**
```
NL: "Place the garden half-hidden, neither fully in front nor locked behind."

→ AB(
    σ(garden): position = side ∪ half_behind(house), glimpse_from(street) = true,
    σ(occupant): privacy = enough, street_contact = retained,
    σ(passerby): garden_glimpse → street_charm += 0.05,
    constraint: garden.exposure = between(private, public)
  )
```

**APL112 ENTRANCE TRANSITION**
```
NL: "Mark the way in with a transition of light, level, and view."

→ AB(
    spawn("transition_zone", pos=between(street, door), state={length: 3-8m}),
    σ(transition): change = count({surface, light, direction, enclosure}) ≥ 2,
    σ(visitor): street_mind → house_mind over transition,
    σ(dwelling): interior_calm += f(transition_depth)
  )
```

**APL113 CAR CONNECTION**
```
NL: "Let the car arrive at a proper place that joins the house without ruling it."

→ AB(
    spawn("car_place", pos=side(house), state={covered: optional, direct_door: kitchen_side}),
    σ(car_place): does_not_interrupt(entrance_transition ∪ garden),
    σ(household): carry_distance(groceries) ≤ 10m,
    σ(street_face): garage_dominance = false
  )
```

**APL114 HIERARCHY OF OPEN SPACE**
```
NL: "Give every outdoor space a protected back and a view onto a larger space."

→ AB(
    σ(open_space): back = protected(wall, slope, hedge), front = looks_onto(larger_space),
    constraint: nesting = chain(seat → terrace → court → street → square),
    σ(agent): position_choice = back_protected, view_to_open,
    σ(comfort): += f(prospect * refuge)
  )
```

**APL115 COURTYARDS WHICH LIVE**
```
NL: "Make courtyards with views out, multiple entries, and sunny edges."

→ AB(
    spawn("courtyard", pos=among(wings), state={openings: ≥ 2, view_out: true, sun: partial}),
    constraint: fully_enclosed_dead_court = false,
    σ(courtyard): routes_cross = natural(paths_through), rooms_open_onto = several,
    σ(agent): if courtyard.alive → use_frequency += 0.3
  )
```

**APL116 CASCADE OF ROOFS**
```
NL: "Step the roofs in a cascade that mirrors the social hierarchy of spaces."

→ AB(
    σ(roofscape): heights = cascade(commons_highest → alcove_lowest),
    σ(roof): each_wing = own_visible_roof,
    σ(observer): building_reading = social_structure(from_roofline),
    constraint: monolithic_flat_top = false
  )
```

**APL117 SHELTERING ROOF**
```
NL: "Let the roof come low and visibly shelter the life beneath it."

→ AB(
    σ(roof): eaves = low(touchable ≤ 2m at_edges), pitch = visible(from_ground),
    σ(interior): top_floor = partly_within(roof_volume),
    σ(resident): sheltered_feeling += f(roof_presence),
    σ(entrance): under(roof_sweep)
  )
```

**APL118 ROOF GARDEN**
```
NL: "Where roofs are flat, make them gardens adjacent to living space."

→ AB ⊗ SD(
    spawn("roof_garden", pos=flat_roof(adjacent_to_rooms), state={access: door_level, planted: true}),
    stock("roof_vegetation", 0.2, [0, 1]),
    flow("planting", void → roof_vegetation, ∂(care * season)),
    σ(roof_garden): usable = true(seats, shade, rail),
    constraint: isolated_ladder_roof = false
  )
```

**APL119 ARCADES**
```
NL: "Run covered arcades along building edges where paths touch them."

→ AB(
    spawn("arcade", pos=edge(building, along_path), state={depth: ≥ 2m, openings: rhythmic}),
    σ(arcade): connects(entrances), shelter = rain ∪ sun,
    σ(pedestrian): if arcade_available → path_choice = arcade (p=0.8),
    σ(building_edge): public_life += arcade_flow * 0.1
  )
```

---

### SEQUENCE 19 — Circulation & Gradient (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ19: Internal circulation, thresholds, and intimacy gradients            │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL120 PATHS AND GOALS**
```
NL: "Lay paths as straight runs between visible intermediate goals."

→ AB(
    σ(walker): heading = toward(next_visible_goal),
    spawn("goal_marker", pos=every(50-100m, along=desire_lines), state={type: tree ∪ gate ∪ seat}),
    σ(path): geometry = polyline(goal_to_goal), forced_curves = false,
    σ(network): emerges_from(walker_traces)
  )
```

**APL121 PATH SHAPE**
```
NL: "Widen paths into place-like shapes where people might pause."

→ AB(
    σ(path): section = varied(narrow_movement ↔ bulged_pause),
    spawn("path_bulge", pos=natural_pause(view, door, corner), state={width: +50%, edge_seat: true}),
    σ(walker): if at(bulge) → pause_probability += 0.2,
    σ(path_life): = movement + lingering
  )
```

**APL122 BUILDING FRONTS**
```
NL: "Bring building fronts to the path edge with individual variation, no uniform setbacks."

→ AB(
    constraint: uniform_setback = false,
    σ(front): position = at_path ± small_variation, doors_windows = onto_path,
    σ(street_wall): rhythm = irregular(alive),
    σ(pedestrian): interest = f(front_variation * opening_density)
  )
```

**APL123 PEDESTRIAN DENSITY**
```
NL: "Size public places so ordinary crowds make them feel alive."

→ AB ⊗ SD(
    constraint: place_area ≤ expected_pedestrians * 15m²,
    stock("liveliness", 0, [0, 1]),
    flow("animation", void → liveliness, ∂(density / optimal_density)),
    σ(place): if area_per_person > 50m² → feels_dead,
    decide(designer, rule=shrink(place) until lively)
  )
```

**APL124 ACTIVITY POCKETS**
```
NL: "Ring public spaces with small activity pockets at their edges."

→ AB ⊗ DE(
    spawn("activity_pocket", pos=edge(public_space), state={type: pick(cafe, stall, seat, game)}),
    σ(public_space): edge_occupancy → center_life,
    queue(visitor, pocket, priority=interest),
    serve(pocket, duration=activity_time),
    constraint: pocket_spacing ≤ 10m along_edge
  )
```

**APL125 STAIR SEATS**
```
NL: "Shape stairs beside public space so they double as seats for watching."

→ AB(
    spawn("stair_seat", pos=level_change(public_space_edge), state={treads: wide, risers: low}),
    σ(stair_seat): orientation = faces(action),
    σ(agent): if watching → sit(stair_seat), vantage = elevated,
    σ(public_space): spectator_ring → performer_energy += 0.1
  )
```

**APL126 SOMETHING ROUGHLY IN THE MIDDLE**
```
NL: "Put something near the middle of a public square, slightly off-center, to gather life."

→ AB(
    spawn("middle_thing", pos=center(square) ± 20%, state={type: fountain ∪ tree ∪ sculpture ∪ bandstand}),
    σ(square): crossing_paths = deflected_around(middle_thing),
    σ(agent): meeting_point = middle_thing (salience),
    σ(middle_thing): touchable = true, climbable = tolerated
  )
```

**APL127 INTIMACY GRADIENT**
```
NL: "Order interior space from public at the door to private at the depths."

→ AB ⊗ DE(
    σ(building): zones = gradient(public → semi_public → semi_private → private),
    route(visitor, condition=relationship_depth, dest=allowed_zone),
    constraint: path_to_private ⊆ passes(semi_public), reverse = false,
    σ(occupant): ease += f(gradient_clarity)
  )
```

**APL128 INDOOR SUNLIGHT**
```
NL: "Place the main daytime rooms on the sunny side."

→ AB(
    σ(day_room): aspect = south, kitchen = east(morning),
    constraint: sun_hours(day_room) ≥ 4h,
    σ(north_side): program = storage ∪ stairs ∪ garage,
    σ(occupant): mood += ∂(sunlit_occupancy)
  )
```

**APL129 COMMON AREAS AT THE HEART**
```
NL: "Put the common area at the heart, where every daily path brushes it."

→ AB(
    σ(common_area): position = crossing(daily_paths), tangent = true(pass_without_entering),
    σ(member): daily_encounters += brush_rate(common_area),
    constraint: common_at_dead_end = false,
    σ(group): cohesion += ∂(chance_meetings)
  )
```

**APL130 ENTRANCE ROOM**
```
NL: "Make the entrance a real room, inside and outside the door at once."

→ AB ⊗ DE(
    spawn("entrance_room", pos=main_door, state={outside_part: porch, inside_part: hall}),
    σ(entrance_room): shelter = waiting(both_sides), coat_zone = inside,
    queue(visitor, entrance_room, priority=arrival),
    serve(greeting, duration=welcome_time),
    σ(threshold): abrupt_door_in_wall = false
  )
```

**APL131 THE FLOW THROUGH ROOMS**
```
NL: "Let movement flow through a loop of rooms, not down dead corridors."

→ AB(
    σ(plan): circulation = through_rooms(loops ≥ 1), corridors = minimized,
    σ(room): doors = placed(corner_flow, generous),
    σ(occupant): route_choice = multiple(loop),
    σ(house): liveliness += f(loop_circulation)
  )
```

---

### SEQUENCE 20 — Rooms & Light (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ20: Room proportions, stairs, and patterned light                       │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL132 SHORT PASSAGES**
```
NL: "Keep passages short, bright, and roomlike."

→ AB(
    constraint: passage.length ≤ 5m,
    σ(passage): daylight = windowed, width = generous, furniture = possible(bookshelf, seat),
    σ(occupant): corridor_dread = 0 if passage ≈ room,
    σ(plan): long_dark_corridor = false
  )
```

**APL133 STAIRCASE AS A STAGE**
```
NL: "Set the stair in view of the common room like a small stage."

→ AB(
    spawn("stair", pos=edge(common_room), state={bottom_steps: flared, visible: true}),
    σ(stair): landing = overlook(commons), sitting = allowed(steps),
    σ(agent): descent = entrance_event(seen),
    σ(children): stair_play = permitted(theatre, watching)
  )
```

**APL134 ZEN VIEW**
```
NL: "Reveal the finest view in glimpses from transitions, never from the main rooms."

→ AB(
    σ(best_view): exposure = narrow(from passage ∪ gate ∪ stair),
    constraint: picture_window_on(best_view) = false,
    σ(occupant): view_value = preserved(∂novelty > 0),
    σ(glimpse): frame = small_opening(along_movement)
  )
```

**APL135 TAPESTRY OF LIGHT AND DARK**
```
NL: "Alternate pools of light with darker passages so light marks the places that matter."

→ AB(
    σ(interior): luminance_field = alternating(bright_places, dim_ground),
    σ(bright_spot): coincides_with(gathering ∪ seat ∪ table),
    σ(agent): attracted_to(light_pools) → settles(intended_places),
    constraint: uniform_illumination = false
  )
```

**APL136 COUPLE'S REALM**
```
NL: "Give the couple a private realm of their own beyond the shared house."

→ AB(
    spawn("couples_realm", pos=private_end(house), state={parts: [bed, sitting, dressing]}),
    σ(realm): door = lockable, children_free_zone = true,
    σ(couple): relationship_time += ∂(realm_use),
    σ(realm): more_than(bedroom) = small_world
  )
```

**APL137 CHILDREN'S REALM**
```
NL: "Give children a realm of their own along a lively circulation path."

→ AB(
    spawn("childrens_realm", pos=strip(kitchen ↔ outdoors), state={parts: [beds, play_corridor, mud_door]}),
    σ(realm): adult_formal_rooms = protected_from(traffic),
    σ(child): range = realm ∪ outdoor_extension, supervision = ambient(kitchen_sightline),
    σ(house): toy_spread = contained(realm)
  )
```

**APL138 SLEEPING TO THE EAST**
```
NL: "Face sleeping rooms east so people wake with the sun."

→ AB(
    σ(bedroom): windows = east(sunrise),
    σ(sleeper): wake = ∂(morning_light), rhythm = circadian_aligned,
    constraint: hot_west_evening_bedroom = avoided,
    σ(bed): placed(morning_ray_reaches)
  )
```

**APL139 FARMHOUSE KITCHEN**
```
NL: "Make the kitchen big and central, the true living room of the house."

→ AB ⊗ DE(
    spawn("farmhouse_kitchen", pos=heart(house), state={table: large_central, hearth_role: true}),
    σ(kitchen): cooking + eating + company = one_room,
    queue(member, kitchen_table, priority=meal ∪ company),
    serve(kitchen, duration=meal_and_talk),
    σ(cook): isolation = false(work_amid_life)
  )
```

**APL140 PRIVATE TERRACE ON THE STREET**
```
NL: "Give the house a raised private terrace that still converses with the street."

→ AB(
    spawn("street_terrace", pos=front(house), state={raised: 0.5m, screen: low_wall ∪ planting}),
    σ(terrace): privacy = partial, street_view = retained,
    σ(resident): evening_sit → greets(passersby), belonging += 0.05,
    σ(street): watched_by(terraces) → safety += 0.05
  )
```

**APL141 A ROOM OF ONE'S OWN**
```
NL: "Give every member of the household a small room of their own."

→ AB(
    constraint: ∀ member: own_room ≥ small(desk, bed ∪ seat, door),
    σ(own_room): control = total(member), retreat = available,
    σ(member): autonomy += 0.1, shared_time = chosen_not_forced,
    σ(household): friction -= f(retreat_availability)
  )
```

**APL142 SEQUENCE OF SITTING SPACES**
```
NL: "Thread varied sitting places through the house, from formal commons to sunny nooks."

→ AB(
    σ(house): sitting_spaces = graded(formal_commons → alcove → window_seat → garden_bench),
    σ(sitting_space): character = f(view, sun, enclosure, group_size),
    σ(occupant): choice = match(mood, space),
    constraint: single_livingroom_monopoly = false
  )
```

---

### SEQUENCE 21 — Sleep & Service (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ21: Bedrooms, baths, workrooms, and service rituals                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL143 BED CLUSTER**
```
NL: "Cluster children's beds in alcoves around a shared playspace."

→ AB(
    spawn("bed_cluster", pos=childrens_realm, state={alcoves: 2-4, commons: play_floor}),
    σ(bed_alcove): private = curtain ∪ half_wall, personal_shelf = true,
    σ(child): sleep = own_nook, waking_play = shared_middle,
    σ(cluster): supervision = single_glance
  )
```

**APL144 BATHING ROOM**
```
NL: "Make one generous bathing room the family's water place, with small toilets elsewhere."

→ AB ⊗ DE(
    spawn("bathing_room", pos=near(couples_realm ∪ bed_clusters), state={tub: large, light: natural, plants: true}),
    σ(bathing): ritual = unhurried(not_utility),
    queue(member, bathing_room, priority=schedule),
    serve(bathing_room, duration=long_soak),
    spawn("small_wc", pos=distributed(house), state={count: ≥ 2})
  )
```

**APL145 BULK STORAGE**
```
NL: "Reserve rough bulk storage — attic, shed, or cellar — for the household's stuff."

→ AB ⊗ DE(
    spawn("bulk_storage", pos=attic ∪ cellar ∪ shed, state={area: ≥ 10%_floor_area, finish: rough}),
    emit("seasonal_swap", time=every(6mo), target=bulk_storage),
    queue(item, bulk_storage, priority=rarity_of_use),
    serve(storage_transfer, duration=short),
    σ(living_rooms): clutter -= f(storage_capacity)
  )
```

**APL146 FLEXIBLE OFFICE SPACE**
```
NL: "Build office space as a loose fabric that workgroups can reshape."

→ AB ⊗ DE(
    spawn("office_fabric", pos=work_building, state={columns: fixed, partitions: movable, services: gridded}),
    emit("reconfigure", time=on(team_change), target=office_fabric),
    serve(reconfiguration, duration=days_not_months),
    σ(workgroup): layout = self_arranged(enclosure ∪ openness),
    constraint: identical_cubicle_grid = false
  )
```

**APL147 COMMUNAL EATING**
```
NL: "Give every group a common table where shared meals hold it together."

→ AB ⊗ DE(
    spawn("common_table", pos=heart(group_space), state={seats: all_members}),
    emit("shared_meal", time=daily ∪ weekly, target=common_table),
    queue(member, common_table, priority=arrival),
    serve(meal, duration=unhurried),
    σ(group): bond += ∂(meals_shared), decisions = often_at(table)
  )
```

**APL148 SMALL WORK GROUPS**
```
NL: "Break work into small groups of a handful of people with their own territory."

→ AB(
    constraint: workgroup.size ≤ 6,
    spawn("group_territory", pos=office_fabric, state={workspaces: member_count, commons: shared_table}),
    σ(worker): belonging = group(known_faces), anonymity = false,
    σ(large_pool): dissolved_into(small_groups)
  )
```

**APL149 RECEPTION WELCOMES YOU**
```
NL: "Make reception a warm welcome, like arriving at someone's home."

→ AB ⊗ DE(
    spawn("reception", pos=entrance_room(work_building), state={form: hearth_like, desk_barrier: false}),
    queue(visitor, reception, priority=arrival),
    serve(reception, duration=personal_greeting),
    σ(reception): offers = seat ∪ drink ∪ orientation,
    σ(visitor): institutional_chill -= 0.2
  )
```

**APL150 A PLACE TO WAIT**
```
NL: "Turn waiting into either engagement or quiet rest, never a dead holding pen."

→ DE(
    spawn("waiting_place", pos=adjacent(service_point), state={modes: [watchable_activity, quiet_alcove]}),
    queue(waiter, service_point, priority=appointment),
    σ(waiter): occupied(watching ∪ reading ∪ dozing) → wait_stress -= 0.3,
    route(waiter, condition=called, dest=service_point),
    constraint: fluorescent_row_of_chairs = false
  )
```

**APL151 SMALL MEETING ROOMS**
```
NL: "Keep meeting rooms small and scattered — most meetings are few people."

→ DE(
    spawn("meeting_room", pos=distributed(work_fabric), state={capacity: ≤ 12, door: near_groups}),
    constraint: majority(meeting_rooms).capacity ≤ 12,
    queue(group, meeting_room, priority=booking),
    serve(meeting_room, duration=agenda_time),
    σ(large_hall): reserved_for(rare_assemblies)
  )
```

---

### SEQUENCE 22 — Work & Independence (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ22: Independent cottages, workshops, and half-private work              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL152 HALF-PRIVATE OFFICE**
```
NL: "Make offices half-private, sheltered but open to easy contact."

→ AB(
    σ(office): enclosure ≈ 0.5(walls_partial, door_optional_open),
    σ(worker): interruptibility = signaled(threshold), contact = low_cost,
    constraint: sealed_box ∨ open_pool = both_false,
    σ(collaboration): += f(visibility * shelter)
  )
```

**APL153 ROOMS TO RENT**
```
NL: "Weave rentable rooms into houses and workplaces for changing needs."

→ AB ⊗ DE(
    spawn("rentable_room", pos=edge(house, own_entrance), state={services: minimal_kitchen ∪ wc}),
    emit("tenancy_change", time=on(demand), target=rentable_room),
    queue(tenant, rentable_room, priority=fit),
    serve(tenancy, duration=lease_term),
    σ(household): income += rent, evolution = flexible
  )
```

**APL154 TEENAGER'S COTTAGE**
```
NL: "Give the teenager a cottage-like room, separate but attached to home."

→ AB(
    spawn("teen_cottage", pos=end(house) ∪ garden_corner, state={own_entrance: true, inside_link: retained}),
    σ(teenager): autonomy += 0.2, family_tie = maintained(shared_meals),
    σ(cottage): convertible = future(guest, office, elder),
    σ(parent): oversight = light(threshold_respect)
  )
```

**APL155 OLD AGE COTTAGE**
```
NL: "House elders in small cottages next to family or neighborhood life."

→ AB(
    spawn("elder_cottage", pos=beside(family_house) ∪ core(neighborhood), state={floor: single, doors: wide}),
    σ(elder): independence = own_household, help = one_call_away,
    constraint: distance(elder_cottage, kin ∪ daily_shops) ≤ 200m,
    σ(street): elder_presence → daytime_eyes += 0.05
  )
```

**APL156 SETTLED WORK**
```
NL: "Let aging workers grow a settled, self-paced form of their work."

→ AB ⊗ SD(
    σ(worker): if aging → work = settled(own_place, own_pace, chosen_craft),
    stock("mastery", skill_level, [0, max]),
    flow("deepening", practice → mastery, ∂(years * reflection)),
    σ(community): elders_working = visible(transmission_to_young),
    constraint: retirement_cliff = false
  )
```

**APL157 HOME WORKSHOP**
```
NL: "Attach a visible workshop to the home where real work happens."

→ AB(
    spawn("home_workshop", pos=front ∪ side(house, street_visible), state={window: work_on_display}),
    σ(worker): commute = 0, household_rhythm = integrated,
    σ(passerby): sees(work) → street_interest += 0.05, local_orders += 0.02,
    σ(child): watches(work) → skill_seed += 0.02
  )
```

**APL158 OPEN STAIRS**
```
NL: "Reach upper units by open outdoor stairs, each with its own door to the street."

→ AB(
    spawn("open_stair", pos=exterior(building), state={serves: ≤ 2_units, roofed: light}),
    σ(upper_unit): own_street_door = true(via_stair),
    constraint: internal_double_loaded_corridor = false,
    σ(resident): territory = continuous(street → stair → door)
  )
```

**APL159 LIGHT ON TWO SIDES OF EVERY ROOM**
```
NL: "Give every room daylight from at least two sides."

→ AB(
    constraint: ∀ room: window_walls ≥ 2,
    σ(room): modeling_light = soft(faces_readable, glare_low),
    σ(plan): consequence = thin_wings ∪ corner_rooms,
    σ(occupant): room_preference ∝ two_sided_light
  )
```

**APL160 BUILDING EDGE**
```
NL: "Thicken the building edge into a zone where life can settle."

→ AB(
    σ(edge): depth > 0(bench, arcade, sill, step) = surface_not_line,
    σ(agent): dwell(edge_zone) → building_alive = true,
    constraint: flat_sheer_wall_to_path = false,
    σ(edge_life): feeds(street_life ↔ interior_life)
  )
```

**APL161 SUNNY PLACE**
```
NL: "Make one sunny outdoor spot the natural magnet of the house edge."

→ AB(
    spawn("sunny_place", pos=south_edge(house, wind_sheltered), state={seat: true, back: wall}),
    σ(sunny_place): attached_to(major_room_door),
    σ(occupant): if sun AND free_time → gravitate(sunny_place),
    σ(house_edge): use_intensity = peak(sunny_place)
  )
```

---

### SEQUENCE 23 — Outdoor Rooms (AB ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ23: Balconies, sunny places, trees, and outdoor occupation              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL162 NORTH FACE**
```
NL: "Treat the cold north face carefully — buffer it and keep its shadow short."

→ AB ⊗ SD(
    σ(north_face): program = buffers(storage, stair, closet), windows = few_small,
    σ(north_ground): shadow_zone = minimized(roof_slopes_north ∪ hardy_planting),
    stock("north_dampness", 0.5, [0, 1]),
    flow("drying", north_dampness → void, ∂(air_flow * design_care)),
    constraint: main_outdoor_life_on_north = false
  )
```

**APL163 OUTDOOR ROOM**
```
NL: "Build outdoor rooms, partly enclosed, roofed by sky or vine."

→ AB(
    spawn("outdoor_room", pos=beside(indoor_commons), state={walls: 2-3_sides, frame: posts ∪ trellis}),
    σ(outdoor_room): floor = finished(paving), furniture = stays_out,
    σ(agent): threshold_cost(indoor → outdoor_room) ≈ 0 → use = daily,
    σ(house): summer_living = shifts(outdoor_room)
  )
```

**APL164 STREET WINDOWS**
```
NL: "Open windows onto the street at the height of passing life."

→ AB(
    σ(street_window): sill = low_enough(seated_view), position = active_rooms,
    σ(resident): street_watching = casual(daily),
    σ(street): watched → safety += 0.05, theatre = mutual,
    constraint: blank_street_wall = false
  )
```

**APL165 OPENING TO THE STREET**
```
NL: "Let ground-floor activities open physically to the street through wide sills and doors."

→ AB(
    σ(ground_floor_use): if public_flavor → opening = wide(counter ∪ folding_door),
    σ(passerby): exchange(words, goods, glances) across_threshold,
    σ(interior): weather_layer = adjustable(seasonal),
    σ(street_life): += ∂(open_frontage_length)
  )
```

**APL166 GALLERY SURROUND**
```
NL: "Wrap upper floors with galleries and balconies that overlook shared life."

→ AB(
    spawn("gallery", pos=surround(upper_floor), state={depth: walkable, rail: leanable}),
    σ(resident): outdoor_access = every_floor,
    σ(gallery): overlooks(court ∪ street) → vertical_community = connected,
    σ(building_face): layered(depth) not_flat
  )
```

**APL167 SIX-FOOT BALCONY**
```
NL: "Make balconies at least six feet deep or they will go unused."

→ AB(
    constraint: balcony.depth ≥ 1.8m,
    σ(balcony): fits(table + chairs + persons ≥ 2), partly_recessed = preferred,
    σ(household): if depth ≥ 1.8m → balcony_use = regular else ornament_only,
    σ(balcony): corner ∪ wall_backed → comfort += 0.1
  )
```

**APL168 CONNECTION TO THE EARTH**
```
NL: "Blur the boundary so the house steps down into its ground."

→ AB ⊗ SD(
    σ(house_edge): terrace = material_flow(indoor_floor → garden_paving),
    σ(threshold): level_change = gentle(low_steps),
    stock("earth_bond", 0, [0, 1]),
    flow("grounding", void → earth_bond, ∂(edge_transitions * plantings_at_edge)),
    σ(occupant): dwelling_rootedness += earth_bond
  )
```

**APL169 TERRACED SLOPE**
```
NL: "Terrace sloping land along the contours for building and growing."

→ AB ⊗ SD(
    σ(slope): form = terraces(contour_following, low_walls),
    stock("topsoil", depth0, [0, max]),
    flow("erosion", topsoil → void, ∂(steepness * rain * (1 - terracing))),
    flow("retention", void → topsoil, ∂(terrace_walls * planting)),
    σ(terrace): use = garden ∪ court ∪ building_shelf
  )
```

**APL170 FRUIT TREES**
```
NL: "Plant fruit trees in commons and gardens as shared seasonal wealth."

→ AB ⊗ SD(
    spawn("fruit_tree", pos=common_land ∪ garden, state={species: climate_fit, age: 0}),
    stock("orchard_yield", 0, [0, max]),
    flow("ripening", void → orchard_yield, ∂(tree_age * season_pulse)),
    σ(resident): harvest = shared(gleaning_allowed), gift_economy += 0.05,
    σ(child): tree_calendar = learned(blossom → fruit)
  )
```

**APL171 TREE PLACES**
```
NL: "Shape space around trees so each becomes a place to be."

→ AB(
    σ(tree): surround = seat ∪ paving_ring ∪ swing = social_anchor,
    constraint: tree_as_leftover_ornament = false,
    σ(agent): shade_seeking → gather(tree_place),
    σ(tree_place): protects(tree_roots) while inviting(people)
  )
```

---

### SEQUENCE 24 — Gardens & Hearth (AB ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ24: Walled gardens, compost cycles, and the hearth center               │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL172 GARDEN GROWING WILD**
```
NL: "Let part of the garden grow wild by its own rules."

→ AB ⊗ SD(
    σ(garden): zones = {tended, wild}, boundary = soft,
    stock("wild_biodiversity", 0.2, [0, 1]),
    flow("succession", void → wild_biodiversity, ∂(non_intervention * years)),
    σ(gardener): control = edges_only(paths_mown),
    σ(child ∪ pollinator): habitat = wild_zone
  )
```

**APL173 GARDEN WALL**
```
NL: "Protect quiet gardens with real walls against noise and wind."

→ AB ⊗ SD(
    spawn("garden_wall", pos=perimeter(garden, exposed_sides), state={height: 1.8m, gates: framed_views}),
    diffuse("street_noise", rate=0.4 * (1 - wall_presence), radius=20),
    σ(garden_interior): stillness += wall_enclosure,
    σ(wall): openings = few(marking_entries), planted_face = true
  )
```

**APL174 TRELLISED WALK**
```
NL: "Cover key garden paths with trellised walks of climbing plants."

→ AB ⊗ SD(
    spawn("trellis_walk", pos=along(main_garden_path), state={frame: light, planted: vine}),
    stock("vine_cover", 0, [0, 1]),
    flow("vine_growth", void → vine_cover, ∂(season * pruning_balance)),
    σ(walker): passage = filtered_light(green_tunnel), pace = slowed,
    σ(garden): rooms = separated_by(trellis_walk)
  )
```

**APL175 GREENHOUSE**
```
NL: "Attach a greenhouse to the house as a live-in edge of growing warmth."

→ AB ⊗ SD(
    spawn("greenhouse", pos=south_wall(house), state={access: from_kitchen, beds: planted}),
    stock("captured_heat", 0, [0, max]),
    flow("solar_gain", void → captured_heat, ∂(insolation * glazing_area)),
    flow("heat_share", captured_heat → house_warmth, ∂(vent_open)),
    σ(household): winter_greens = supplied, tending = daily_pleasure
  )
```

**APL176 GARDEN SEAT**
```
NL: "Hide one quiet garden seat where a person can be alone with the growing world."

→ AB(
    spawn("garden_seat", pos=secluded_corner(garden), state={back: sheltered, view: planting}),
    σ(occupant): solitude = protected(not_overlooked),
    σ(agent): if reflection_need > 0.6 → seek(garden_seat), calm += 0.1,
    constraint: count(garden_seat) ≥ 1 per_garden
  )
```

**APL177 VEGETABLE GARDEN**
```
NL: "Keep a working vegetable garden within every household's reach."

→ AB ⊗ SD(
    spawn("vegetable_garden", pos=sunny_patch(lot ∪ common_land), state={beds: raised ∪ row}),
    stock("harvest", 0, [0, seasonal_max]),
    flow("growth", void → harvest, ∂(soil_fertility * care * season)),
    flow("consumption", harvest → household_food, ∂(meal_demand)),
    σ(gardener): food_source_knowledge += 0.1
  )
```

**APL178 COMPOST**
```
NL: "Return organic waste to the soil through visible compost cycles."

→ SD(
    stock("compost_pile", 0, [0, bin_capacity]),
    flow("kitchen_scraps", household_waste → compost_pile, ∂(meals * sorting_habit)),
    flow("decomposition", compost_pile → humus, ∂(turn_rate * moisture)),
    flow("return", humus → soil_fertility, ∂(spreading_schedule)),
    feedback(soil_fertility, +, garden_yield, delay=1_season)
  )
```

**APL179 ALCOVES**
```
NL: "Open small alcoves off common rooms so people can be together yet apart."

→ AB(
    spawn("alcove", pos=edge(common_room), state={width: ~2m, ceiling: lowered, opening: full}),
    σ(alcove): holds(2-4), activity = reading ∪ talk ∪ game,
    σ(member): partial_privacy(in_view, out_of_flow),
    σ(common_room): life = center + occupied_edges
  )
```

**APL180 WINDOW PLACE**
```
NL: "Make each favored window a place — seat, ledge, and light to inhabit."

→ AB(
    spawn("window_place", pos=best_light(room), state={seat: built_in, sill: low, depth: reveal}),
    σ(occupant): window_pull + comfort_pull = resolved(one_place),
    σ(agent): if in(room) → likely_position = window_place,
    constraint: room_without_window_place = incomplete
  )
```

**APL181 THE FIRE**
```
NL: "Keep one live flame around which the household gathers."

→ AB ⊗ SD(
    spawn("hearth", pos=commons(house), state={fire: contained, seats: arc}),
    stock("ember_glow", 0, [0, 1]),
    flow("stoking", fuel → ember_glow, ∂(tending)),
    flow("dying", ember_glow → void, ∂(time)),
    σ(household): evening_gathering ∝ ember_glow, stories += ∂(fire_hours)
  )
```

**APL182 EATING ATMOSPHERE**
```
NL: "Center the meal in warm low light around a heavy shared table."

→ AB(
    σ(eating_place): table = solid(center), light = pooled(low_lamp), walls = near(enclosing),
    σ(meal): pace = unhurried, ceremony = slight(candle, serving),
    σ(diner): conviviality += f(light_pool * enclosure),
    constraint: eating_in_glare ∨ scatter = false
  )
```

---

### SEQUENCE 25 — Workspace Layout (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ25: Workspaces shaped by enclosure, reach, and use                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL183 WORKSPACE ENCLOSURE**
```
NL: "Wrap each workspace with about half-enclosure — protected back, open front."

→ AB(
    σ(workspace): enclosure ∈ [0.5, 0.75](back_wall, side_screen, open_face),
    σ(worker): concentration += shelter, contact = retained(open_front),
    σ(desk): no_one_directly_behind = true,
    constraint: full_box ∨ open_floor = both_false
  )
```

**APL184 COOKING LAYOUT**
```
NL: "Arrange the cooking around the cook — stove, sink, and counters within a few steps."

→ AB(
    σ(kitchen): stations = {stove, sink, store, counter} within_radius(3m),
    σ(cook): path = short_arcs(not_corridor_runs),
    σ(counter): continuous = generous(spread_out_work),
    σ(helper): second_worker = fits(without_collision)
  )
```

**APL185 SITTING CIRCLE**
```
NL: "Pull the chairs into a rough circle where every seat sees every face."

→ AB(
    σ(sitting_group): geometry = rough_circle(diameter 2.5-3.5m),
    σ(seat): mixed(chairs, cushions, bench_ends), backs = protected,
    σ(talker): eye_contact = all_pairs → conversation_flow += 0.2,
    constraint: row_facing_one_direction = false
  )
```

**APL186 COMMUNAL SLEEPING**
```
NL: "Allow shared sleeping arrangements when the group wants to sleep near each other."

→ AB(
    σ(sleeping): mode = optional_commons(platform ∪ mats) beside private_alcoves,
    σ(household): choice = per_night(social_sleep ↔ solitude),
    σ(guest): absorbed_easily(communal_floor),
    σ(child): comfort += ∂(near_others_at_night)
  )
```

**APL187 MARRIAGE BED**
```
NL: "Build the couple's bed as a crafted place, enclosed and their own."

→ AB(
    spawn("marriage_bed", pos=center(couples_realm), state={frame: built(canopy ∪ alcove), scale: generous}),
    σ(bed): enclosure = partial(head_sheltered), dawn_light = reaches,
    σ(couple): bond += ∂(place_made_together),
    constraint: bed_as_furniture_afterthought = false
  )
```

**APL188 BED ALCOVE**
```
NL: "Set each bed in its own alcove rather than loose in a big room."

→ AB(
    spawn("bed_alcove", pos=edge(sleeping_area), state={walls: 3_sides, ceiling: low, shelf: personal}),
    σ(sleeper): enclosure → sleep_security += 0.1,
    σ(bedroom): freed_floor = play ∪ dressing,
    σ(alcove): curtain = closable(own_tiny_room)
  )
```

**APL189 DRESSING ROOM**
```
NL: "Give dressing its own small room of light, mirror, and hanging space."

→ AB(
    spawn("dressing_room", pos=between(bed_realm, bathing_room), state={area: 2-4m², light: natural ∪ warm}),
    σ(dressing_room): storage = open_rails ∪ shelves(clothes_visible),
    σ(occupant): morning_flow = bed → dress → out(no_backtrack),
    σ(bedroom): wardrobe_clutter = removed
  )
```

---

### SEQUENCE 26 — Volume & View (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ26: Interior volume, outlook, and spatial prospect                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL190 CEILING HEIGHT VARIETY**
```
NL: "Vary ceiling heights with the social intimacy of each room."

→ AB(
    σ(room): ceiling_height = f(social_distance): commons ≈ 3-3.5m, family ≈ 2.5m, alcove ≈ 2m,
    constraint: uniform_slab_height = false,
    σ(occupant): felt_intimacy = matched(height, group_size),
    σ(section): becomes = landscape(high_and_low)
  )
```

**APL191 THE SHAPE OF INDOOR SPACE**
```
NL: "Keep rooms roughly rectangular with rough symmetry, no wild acute angles."

→ AB(
    σ(room): plan = near_rectangle(angles ≈ 90° ± tolerance), ceiling = vault ∪ soft_flat,
    constraint: sharp_acute_corners = avoided(dead_space),
    σ(walls): straight_enough(furniture_fits), slight_irregularity = warm,
    σ(occupant): spatial_ease += f(shape_calm)
  )
```

**APL192 WINDOWS OVERLOOKING LIFE**
```
NL: "Aim windows at living scenes, not blank walls or empty sky."

→ AB(
    σ(window): target = scene(street ∪ garden ∪ court_activity),
    constraint: window_onto(blank_wall) = false,
    σ(occupant): glance_out = frequent(connection_maintained),
    σ(room): if window_scene.alive → occupancy += 0.1
  )
```

**APL193 HALF-OPEN WALL**
```
NL: "Divide connected rooms with half-open walls — part screen, part opening."

→ AB(
    σ(partition): form = half(counter ∪ columns ∪ shelf ∪ wide_opening),
    σ(rooms): joined(sight, voice) while distinct(zone, warmth),
    σ(occupant): togetherness ⊗ separateness = balanced,
    constraint: binary(full_wall ∨ no_wall) = false
  )
```

**APL194 INTERIOR WINDOWS**
```
NL: "Pierce interior walls with windows between rooms and passages."

→ AB(
    spawn("interior_window", pos=wall(room ↔ hall ∪ room ↔ room), state={glazed: true}),
    σ(passage): borrowed_light += window_area,
    σ(occupant): awareness(adjacent_life) without sound_transfer,
    σ(house): depth_feels = connected(layered_views)
  )
```

**APL195 STAIRCASE VOLUME**
```
NL: "Size the stair as a real volume of the building, neither cramped nor wasteful."

→ AB(
    σ(stair_volume): planned = early(design_not_afterthought),
    σ(stair): width = generous_but_tight, light = windowed, landing = pause_place,
    constraint: stair_as_residual_shaft = false,
    σ(builder): volume_check = treads × width × height_envelope
  )
```

**APL196 CORNER DOORS**
```
NL: "Place room doors near corners so the room's body stays whole."

→ AB(
    σ(door): position = corner(room), swing = against_wall,
    σ(room): circulation = clipped(corner_arc), furniture_field = uninterrupted,
    constraint: door_mid_wall(cuts_room) = avoided,
    σ(flow): rooms_chain = corner_to_corner
  )
```

---

### SEQUENCE 27 — Volume & View (alt) (AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ27: Alternate spatial readings of volume, prospect, and refuge          │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Patterns: APL190–APL196**
```
→ AB:
  σ(room): layers = sequence(threshold, interior, prospect),
  σ(opening): reveal = deep, seating = integrated_when_possible,
  σ(path): compress_then_release(at_major_views),
  σ(occupant): attention = drawn_toward(light_and_distant_outlook)
```

---

### SEQUENCE 28 — Thick Walls & Storage (AB ⊗ DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ28: Thickened edges, closets, storage, and built-in detail              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL197 THICK WALLS**
```
NL: "Make walls deep enough to hold niches, seats, and shelves."

→ AB(
    σ(wall): depth ≥ 30cm(usable), character = carved(volume_not_membrane),
    spawn("wall_niche", pos=within(thick_wall), state={use: seat ∪ shelf ∪ cupboard}),
    σ(occupant): personalization = carving(edges_of_rooms),
    σ(room): edge_depth → life_at_edges += 0.1
  )
```

**APL198 CLOSETS BETWEEN ROOMS**
```
NL: "Use closets as thick sound-buffering layers between rooms."

→ AB ⊗ SD(
    σ(closet): position = party_wall(room ↔ room), depth = storage,
    diffuse("sound", rate=0.2 * (1 - closet_layer), radius=adjacent_room),
    σ(rooms): acoustic_privacy += closet_buffer,
    constraint: closet_on_outside_wall(steals_light) = avoided
  )
```

**APL199 SUNNY COUNTER**
```
NL: "Run the kitchen counter along the sunny window side."

→ AB(
    σ(counter): position = under(south ∪ east_window),
    σ(cook): work_in_sunlight → morning_mood += 0.1,
    σ(kitchen): plants_on_sill = thrive,
    constraint: counter_in_dark_core = false
  )
```

**APL200 OPEN SHELVES**
```
NL: "Store daily things on open shelves one layer deep."

→ AB(
    σ(shelf): open = true, depth = single_row(everything_visible),
    σ(occupant): retrieval = one_glance_one_reach,
    σ(kitchen ∪ workroom): used_objects = display(patina_of_use),
    constraint: deep_closed_cabinets = only_for(rare_items)
  )
```

**APL201 WAIST-HIGH SHELF**
```
NL: "Run a waist-high shelf around rooms for the traffic of daily objects."

→ AB(
    spawn("waist_shelf", pos=perimeter(active_rooms), state={height: ~0.9m, depth: ~30cm}),
    σ(object): resting_place = waist_shelf(keys, letters, bowls),
    σ(occupant): put_down ∪ pick_up = effortless,
    σ(room): table_clutter -= 0.1
  )
```

**APL202 BUILT-IN SEATS**
```
NL: "Build seats into the fabric — window sills, walls, chimney corners."

→ AB ⊗ DE(
    emit("build_seats", time=with(structure_finish), payload={locations: [window, hearth, stair, wall_niche]}),
    serve(seat_construction, duration=per_seat),
    σ(built_seat): cushioned = later(inhabitants), always_there = true,
    σ(room): seating_minimum = met_without_furniture
  )
```

**APL203 CHILD CAVES**
```
NL: "Leave small cave-like places scaled to children."

→ AB(
    spawn("child_cave", pos=under(stair ∪ counter ∪ eave), state={ceiling: ≤ 1.2m, entry: small}),
    σ(child): claims(cave) → imaginative_play += 0.2, ownership_feeling += 0.1,
    σ(adult): entry = impossible(by_design),
    σ(house): tolerates(secret_geography_of_children)
  )
```

**APL204 SECRET PLACE**
```
NL: "Hide one secret place in every house for precious things."

→ AB(
    spawn("secret_place", pos=concealed(wall ∪ floor ∪ stair), state={known_to: household_only}),
    σ(household): keeps(treasures, letters, small_rituals),
    σ(house): mystery += 1(place_beyond_plan),
    σ(occupant): attachment_to_house += 0.05
  )
```

---

### SEQUENCE 29 — Structural Principle (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ29: Structure emerges from social space and build order                 │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL205 STRUCTURE FOLLOWS SOCIAL SPACES**
```
NL: "Derive the structure from the social spaces — never force rooms into a structural grid."

→ DE(
    emit("read_social_plan", time=0, payload={spaces: designed_life}),
    route(structural_element, condition=space_boundary, dest=column ∪ wall_position),
    serve(structural_design, duration=fit_time),
    constraint: grid_first_rooms_later = false,
    σ(structure): each_space = own_structural_bay(integrity)
  )
```

**APL206 EFFICIENT STRUCTURE**
```
NL: "Let the building work as a compressive whole — walls, floors, and roof sharing load."

→ DE ⊗ SD(
    σ(structure): system = continuous(walls_bear, vaults_span),
    stock("material_use", 0, [0, budget]),
    flow("consumption", budget → material_use, ∂(member_sizing)),
    feedback(load_path_efficiency, -, material_use, delay=design_iteration),
    constraint: skeleton_plus_curtain = reconsidered(when_compression_possible)
  )
```

**APL207 GOOD MATERIALS**
```
NL: "Choose materials that are ecological, workable by hand, and good to touch."

→ DE(
    route(material_choice, condition={local, low_energy, repairable, ages_well}, dest=approved_palette),
    constraint: toxic ∪ unrepairable ∪ alien_to_hand = rejected,
    serve(procurement, duration=local_supply_time),
    σ(building): weathering = graceful(patina_not_decay)
  )
```

**APL208 GRADUAL STIFFENING**
```
NL: "Erect a light frame first, then stiffen it gradually as the building takes shape."

→ DE(
    emit("erect_light_frame", time=0, payload={tolerance: adjustable}),
    emit("infill_and_stiffen", time=after(frame), payload={material: fill_hardens}),
    emit("final_stiffening", time=after(infill), payload={connections: locked}),
    σ(builder): adjustment = possible_late(fit_to_life),
    σ(structure): stiffness(t) = monotonic_increasing
  )
```

---

### SEQUENCE 30 — Roof & Floor Layout (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ30: Ordering roofs, floors, and column placement                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL209 ROOF LAYOUT**
```
NL: "Lay out the roofs first as a system of vaults and caps over the wings."

→ DE(
    emit("layout_roofs", time=after(plan_freeze), payload={form: vault ∪ pitched, per: wing}),
    route(roof_piece, condition=wing_geometry, dest=matching_vault),
    serve(roof_design, duration=layout_time),
    constraint: leftover_unroofable_corners = none,
    σ(roofscape): reads = cascade(APL116_consistent)
  )
```

**APL210 FLOOR AND CEILING LAYOUT**
```
NL: "Lay out floor and ceiling vaults so each room's height fits its use."

→ DE(
    emit("layout_floors", time=with(roof_layout), payload={vaults: room_by_room}),
    route(ceiling_height, condition=room_intimacy, dest=vault_profile),
    serve(floor_ceiling_design, duration=per_floor),
    constraint: heights = APL190_consistent,
    σ(section): stacking = resolved(no_wasted_interstitial)
  )
```

**APL211 THICKENING THE OUTER WALLS**
```
NL: "Thicken outer walls where depth is needed for the life of the edge."

→ DE(
    emit("mark_thickening", time=after(room_layout), payload={zones: window_places ∪ niches ∪ seats}),
    route(wall_segment, condition=edge_use_intensity, dest=thick_profile),
    serve(wall_forming, duration=per_segment),
    σ(wall): thickness = varies(30-80cm), volume = usable(APL197_consistent)
  )
```

**APL212 COLUMNS AT THE CORNERS**
```
NL: "Fix columns first at the corners of every social space."

→ DE(
    emit("place_corner_columns", time=structure_start, payload={at: room_corners}),
    route(column, condition=corner(social_space), dest=exact_corner),
    serve(column_erection, duration=per_column),
    σ(room): corners = held(structure_space_agreement),
    constraint: column_in_middle_of_room_edge = later_only
  )
```

**APL213 FINAL COLUMN DISTRIBUTION**
```
NL: "Distribute remaining columns by the load — closer where loads are heavy."

→ DE ⊗ SD(
    stock("load_map", roof_and_floor_loads, [0, max]),
    emit("compute_spacing", time=after(corner_columns), payload={rule: spacing ∝ 1/load}),
    route(intermediate_column, condition=span > allowable, dest=subdivision_point),
    serve(final_placement, duration=per_column),
    σ(structure): column_field = graded(dense_low_floors, sparse_top)
  )
```

---

### SEQUENCE 31 — Foundation & Frame (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ31: Sequencing foundations, columns, beams, and vaults                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL214 ROOT FOUNDATIONS**
```
NL: "Root the columns into the ground with foundations that grip like roots."

→ DE(
    emit("excavate_pads", time=0, payload={at: column_positions, depth: below_frost}),
    emit("pour_root", time=after(excavation), payload={form: splayed(grip), tie: column_base}),
    serve(foundation_cure, duration=cure_time),
    σ(column_base): continuity = monolithic(ground ↔ column),
    constraint: loose_pinned_bases = false
  )
```

**APL215 GROUND FLOOR SLAB**
```
NL: "Cast a simple ground slab raised slightly above the earth around it."

→ DE(
    emit("prepare_bed", time=after(foundations), payload={fill: compacted, drainage: true}),
    emit("cast_slab", time=after(bed), payload={level: +30cm_above_grade}),
    serve(slab_cure, duration=cure_time),
    σ(slab): edge = becomes(low_plinth ∪ sitting_step),
    σ(ground_floor): dryness = ensured(rise_above_grade)
  )
```

**APL216 BOX COLUMNS**
```
NL: "Build columns as hollow boxes filled on site — light to raise, strong when filled."

→ DE(
    emit("assemble_box", time=column_stage, payload={skin: wood ∪ board, hollow: true}),
    emit("raise_column", time=after(assembly), payload={crew: 2, weight: light}),
    emit("fill_column", time=after(raising), payload={fill: concrete ∪ earth_mix}),
    serve(column_fill_cure, duration=cure_time),
    σ(column): final = composite(skin_as_formwork_stays)
  )
```

**APL217 PERIMETER BEAMS**
```
NL: "Ring each floor with continuous perimeter beams tying columns together."

→ DE(
    emit("form_beams", time=after(columns), payload={path: perimeter(each_room_group)}),
    emit("cast_beams", time=after(forming), payload={continuity: ring}),
    serve(beam_cure, duration=cure_time),
    σ(beam_ring): braces(columns), receives(vault_thrust),
    constraint: discontinuous_lintels_only = false
  )
```

**APL218 WALL MEMBRANES**
```
NL: "Span walls between columns as thin stressed membranes, not heavy infill."

→ DE(
    emit("stretch_membrane", time=after(beams), payload={skin: lath ∪ mesh, core: light_fill}),
    serve(membrane_finish, duration=per_wall),
    σ(wall): works_with(columns, beams) = one_shell,
    σ(membrane): thickened_locally(APL211_zones),
    constraint: wall_as_independent_heavy_leaf = false
  )
```

**APL219 FLOOR-CEILING VAULTS**
```
NL: "Span floors and ceilings as shallow vaults that work in compression."

→ DE(
    emit("form_vault", time=after(walls_below), payload={profile: shallow_catenary}),
    emit("cast_vault", time=after(forming), payload={layers: thin_shell}),
    serve(vault_cure, duration=cure_time),
    σ(vault): underside = becomes(ceiling_shape, APL190_consistent),
    σ(floor_above): rides(levelled_fill_over_vault)
  )
```

**APL220 ROOF VAULTS**
```
NL: "Crown the building with roof vaults shaped by the hanging chain."

→ DE(
    emit("find_curve", time=roof_stage, payload={method: catenary(hang_chain_invert)}),
    emit("form_roof_vault", time=after(curve), payload={span: per_wing}),
    emit("cast_and_weather", time=after(forming), payload={skin: waterproof_layers}),
    serve(roof_cure, duration=cure_time),
    σ(roof): pure_compression = true, sheltering_form = APL117_consistent
  )
```

---

### SEQUENCE 32 — Openings (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ32: Constructing windows, doors, and light-admitting cuts               │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL221 NATURAL DOORS AND WINDOWS**
```
NL: "Let each opening find its own size and position from the life inside — no two identical."

→ DE(
    emit("mark_openings", time=after(walls_standing), payload={by: standing_inside(looking_out)}),
    route(opening, condition=view ∪ light ∪ passage_need, dest=marked_position),
    serve(opening_cut, duration=per_opening),
    constraint: identical_repeated_windows = false,
    σ(facade): order = deeper(inner_life_not_grid)
  )
```

**APL222 LOW SILL**
```
NL: "Keep sills low so the seated eye still owns the ground outside."

→ DE(
    emit("set_sills", time=window_stage, payload={height: seated_eye_rule}),
    σ(sill): height = 30-40cm(ground_floor), slightly_higher(upper_floors),
    serve(sill_build, duration=per_window),
    σ(seated_occupant): view = includes(ground, garden, street)
  )
```

**APL223 DEEP REVEALS**
```
NL: "Splay the window reveals so light grades softly into the room."

→ DE(
    emit("splay_reveals", time=with(opening_cut), payload={angle: 50-60°, depth: wall_thickness}),
    serve(reveal_plaster, duration=per_window),
    σ(reveal): luminance_gradient = soft(glare -= 0.3),
    σ(window): reads = generous(thickness_shown)
  )
```

**APL224 LOW DOORWAY**
```
NL: "Mark special transitions with a low door that asks a slight bow."

→ DE(
    emit("set_low_door", time=door_stage, payload={at: private ∪ sacred_thresholds, height: ≈ 1.8m}),
    serve(door_fit, duration=per_door),
    σ(passer): slight_bow → threshold_awareness += 0.1,
    constraint: main_public_doors = full_height(low_only_where_special)
  )
```

**APL225 FRAMES AS THICKENED EDGES**
```
NL: "Grow frames out of the wall as thickened edges, not stuck-on trim."

→ DE(
    emit("form_frames", time=with(opening_cut), payload={method: build_up(wall_material_around_opening)}),
    serve(frame_build, duration=per_opening),
    σ(frame): monolithic_with(wall) = true, depth = structural,
    constraint: thin_applied_frame = false
  )
```

---

### SEQUENCE 33 — Columns & Services (DE ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ33: Structural expression coupled to heat and service flows             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL226 COLUMN PLACE**
```
NL: "Make freestanding columns thick enough to be places people touch and lean on."

→ AB(
    σ(column): girth = generous(≥ 30cm), surface = touchable,
    spawn("column_place", pos=freestanding_column, state={seat: base_ring ∪ ledge}),
    σ(agent): lean ∪ sit(column_place) → column = social_anchor,
    constraint: thin_pipe_column = false
  )
```

**APL227 COLUMN CONNECTIONS**
```
NL: "Thicken and brace the column-beam joints so forces flow visibly."

→ DE(
    emit("form_connections", time=after(columns_and_beams), payload={form: splayed_capital ∪ brace}),
    serve(connection_build, duration=per_joint),
    σ(joint): stiffness = increased(moment_capacity),
    σ(observer): load_path = legible(structure_understood)
  )
```

**APL228 STAIR VAULT**
```
NL: "Build the stair over its own small vault of compressive material."

→ DE(
    emit("form_stair_vault", time=stair_stage, payload={curve: inclined_catenary}),
    emit("cast_steps", time=after(vault), payload={treads: over_vault}),
    serve(stair_cure, duration=cure_time),
    σ(understair): volume = usable(child_cave ∪ storage, APL203_consistent)
  )
```

**APL229 DUCT SPACE**
```
NL: "Run services in a dedicated duct space at the vault's edge."

→ DE ⊗ SD(
    emit("reserve_ducts", time=with(floor_vaults), payload={zone: spandrel(vault ↔ floor)}),
    route(service_run, condition=type(water, power, air), dest=duct_space),
    serve(duct_install, duration=per_run),
    stock("service_capacity", installed, [0, duct_volume]),
    σ(maintenance): access = panel(no_wall_demolition)
  )
```

**APL230 RADIANT HEAT**
```
NL: "Heat people with warm radiant surfaces, not blasts of hot air."

→ SD(
    stock("surface_warmth", cold, [cold, warm]),
    flow("heating", source → surface_warmth, ∂(radiant_input)),
    flow("radiation", surface_warmth → occupant_comfort, ∂(view_factor * ΔT)),
    feedback(occupant_comfort, -, heating_input, delay=thermostat_lag),
    σ(occupant): comfort = radiant(cooler_air_acceptable)
  )
```

**APL231 DORMER WINDOWS**
```
NL: "Puncture the roof with dormers so rooms within the roof get light and air."

→ DE(
    emit("frame_dormers", time=roof_stage, payload={at: rooms_in_roof, count: ≥ 1_per_room}),
    serve(dormer_build, duration=per_dormer),
    σ(roof_room): daylight += dormer_area, headroom += dormer_volume,
    σ(roofscape): rhythm = enlivened(dormers)
  )
```

**APL232 ROOF CAPS**
```
NL: "Finish roof ridges and edges with caps that crown the building."

→ DE(
    emit("place_caps", time=roof_completion, payload={at: ridge ∪ eave_ends, form: modeled}),
    serve(cap_build, duration=per_cap),
    σ(silhouette): terminated = deliberate(sky_meeting),
    σ(building): completion_signal = caps_on
  )
```

---

### SEQUENCE 34 — Surfaces & Finishes (DE)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ34: Floors, cladding, plaster, trim, and filtered light                │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL233 FLOOR SURFACE**
```
NL: "Grade floor surfaces from hard public edges to soft intimate interiors."

→ DE(
    emit("lay_floors", time=finish_stage, payload={gradient: hard(entrance) → soft(inner_rooms)}),
    route(floor_material, condition=zone_intimacy, dest={stone ∪ tile: public, wood ∪ soft: private}),
    serve(floor_laying, duration=per_room),
    σ(occupant): transition = felt(surface_change_marks_zones)
  )
```

**APL234 LAPPED OUTSIDE WALLS**
```
NL: "Clad exterior walls in lapped layers that shed water and show their making."

→ DE(
    emit("clad_walls", time=after(membranes), payload={method: lapped(boards ∪ shingles ∪ tiles)}),
    serve(cladding, duration=per_wall),
    σ(skin): sheds(rain, gravity_wise), repair = piece_by_piece,
    constraint: sealed_membrane_dependence = false
  )
```

**APL235 SOFT INSIDE WALLS**
```
NL: "Finish inside walls soft and warm to the touch."

→ DE(
    emit("finish_interiors", time=after(cladding), payload={material: soft_plaster ∪ wood}),
    serve(interior_finish, duration=per_room),
    σ(wall_surface): touch = warm(slightly_yielding), light = diffused,
    constraint: hard_cold_gloss = avoided
  )
```

**APL236 WINDOWS WHICH OPEN WIDE**
```
NL: "Make windows that open the whole wall to the air when weather allows."

→ DE(
    emit("fit_openers", time=window_stage, payload={type: casement ∪ folding(full_aperture)}),
    serve(window_fit, duration=per_window),
    σ(window): open_area / frame_area ≥ 0.8,
    σ(room): summer_mode = merges_with(outdoors)
  )
```

**APL237 SOLID DOORS WITH GLASS**
```
NL: "Give doors solid weight with a glimpse of glass."

→ DE(
    emit("hang_doors", time=door_stage, payload={leaf: solid, lite: small_glass}),
    serve(door_fit, duration=per_door),
    σ(door): sound = damped(solid), approach = seen(glass),
    σ(user): collision_avoided ∪ greeting_prepared
  )
```

**APL238 FILTERED LIGHT**
```
NL: "Filter incoming light through fine structure — vines, lattice, thin curtains."

→ DE ⊗ SD(
    emit("add_filters", time=finish_stage, payload={layers: trellis ∪ lattice ∪ sheer}),
    stock("glare", raw_sunlight, [0, max]),
    flow("filtering", glare → soft_light, ∂(filter_density)),
    σ(room): light_texture = dappled(alive),
    σ(occupant): eye_strain -= 0.1
  )
```

**APL239 SMALL PANES**
```
NL: "Divide glazing into small panes that make the view precious."

→ DE(
    emit("glaze_windows", time=window_stage, payload={panes: small(≤ 30cm), muntins: fine}),
    serve(glazing, duration=per_window),
    σ(view): framed_many_times → intensity += 0.1,
    σ(glass): replacement = cheap(single_pane_scale)
  )
```

**APL240 HALF-INCH TRIM**
```
NL: "Cover every joint of materials with modest half-inch trim."

→ DE(
    emit("run_trim", time=final_finish, payload={size: ≈ 12mm, at: every_material_junction}),
    serve(trim_fit, duration=per_edge),
    σ(junction): tolerance = absorbed(clean_line),
    σ(finish): hand_scale = everywhere(small_repeated_detail)
  )
```

---

### SEQUENCE 35 — Outdoor Details (DE ⊗ SD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ35: Seats, paving, shade, and plant growth around buildings             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL241 SEAT SPOTS**
```
NL: "Put outdoor seats exactly where sun, view, and activity already invite sitting."

→ AB ⊗ DE(
    sense(site, radius=all, property={sun_hours, view_value, passing_life}),
    spawn("seat_spot", pos=argmax(sun ⊗ view ⊗ activity), state={type: bench ∪ ledge}),
    emit("seasonal_review", time=every(season), target=seat_spots),
    σ(agent): seat_use = f(placement_quality), unused_seat → relocate
  )
```

**APL242 FRONT DOOR BENCH**
```
NL: "Set a bench by the front door facing the street's life."

→ AB(
    spawn("door_bench", pos=beside(front_door, sunny_side), state={back: house_wall}),
    σ(resident): sits(door_bench) → threshold_life += 0.1, neighbor_talk += 0.05,
    σ(street): bench_occupancy → watched_street(safety += 0.05),
    σ(bench): half_private(house_territory, public_view)
  )
```

**APL243 SITTING WALL**
```
NL: "Build low walls at sitting height wherever territories meet."

→ AB ⊗ SD(
    spawn("sitting_wall", pos=boundary(court ↔ path ∪ garden ↔ street), state={height: ≈ 40cm, top: wide_smooth}),
    σ(wall): divides(zones) while joining(people_sit_both_sides),
    stock("boundary_softness", 0, [0, 1]),
    flow("softening", void → boundary_softness, ∂(sitting_use)),
    σ(child): balance_walk(sitting_wall) = play
  )
```

**APL244 CANVAS ROOFS**
```
NL: "Stretch canvas awnings for movable seasonal shelter."

→ DE ⊗ SD(
    emit("rig_canvas", time=warm_season, payload={over: terrace ∪ court ∪ walk}),
    emit("strike_canvas", time=cold_season, target=canvas_roofs),
    stock("shade_comfort", 0, [0, 1]),
    flow("shading", void → shade_comfort, ∂(canvas_area * sun_intensity)),
    σ(space): character = softened(light_through_cloth)
  )
```

**APL245 RAISED FLOWERS**
```
NL: "Raise flower beds to touch and smell height along walks and windows."

→ AB ⊗ SD(
    spawn("raised_bed", pos=edge(path ∪ window ∪ seat), state={height: 40-60cm}),
    stock("bloom", 0, [0, 1]),
    flow("flowering", void → bloom, ∂(season * tending)),
    σ(passerby): eye_level_flowers → pause += 0.1, scent_contact = direct,
    σ(gardener): tending = back_unbent
  )
```

**APL246 CLIMBING PLANTS**
```
NL: "Train climbing plants over walls and trellises to knit building and garden."

→ SD(
    stock("vine_coverage", 0, [0, wall_area]),
    flow("climbing_growth", void → vine_coverage, ∂(species_rate * support_area)),
    flow("pruning", vine_coverage → void, ∂(maintenance_schedule)),
    feedback(vine_coverage, +, facade_softness, delay=seasons),
    σ(wall): alive = seasonal(green → autumn_color)
  )
```

**APL247 PAVING WITH CRACKS BETWEEN THE STONES**
```
NL: "Pave with open joints where grass and moss can grow."

→ AB ⊗ SD(
    spawn("paving", pos=paths ∪ terrace, state={units: stones, joints: soil_open}),
    stock("joint_growth", 0, [0, 1]),
    flow("colonization", void → joint_growth, ∂(moisture / foot_traffic)),
    σ(ground): permeable = true(rain_absorbed),
    σ(path_character): softened(half_green)
  )
```

**APL248 SOFT TILE AND BRICK**
```
NL: "Fire tiles and bricks soft enough to wear smooth underfoot with time."

→ DE ⊗ SD(
    emit("fire_units", time=material_prep, payload={temperature: low(soft_biscuit)}),
    serve(laying, duration=per_surface),
    stock("wear_polish", 0, [0, 1]),
    flow("wearing", void → wear_polish, ∂(footsteps * softness)),
    σ(surface): records(use) → warmth_of_age += wear_polish
  )
```

---

### SEQUENCE 36 — Ornament & Life (DE ⊗ AB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ SEQ36: Ornament, color, artifacts, and visible traces of life              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**APL249 ORNAMENT**
```
NL: "Apply ornament at edges, joints, and transitions to hold them together."

→ DE ⊗ AB(
    emit("apply_ornament", time=after(trim), payload={at: seams(door, window, eave, gate)}),
    serve(ornament_work, duration=per_seam),
    σ(ornament): role = binding(transition_smoothed), maker = inhabitant_hand_welcome,
    σ(observer): eye_rests(ornamented_joint) → coherence += 0.05
  )
```

**APL250 WARM COLORS**
```
NL: "Choose surface colors that make the light in rooms warm."

→ AB(
    σ(palette): base = warm(earth, ochre, rose, wood),
    constraint: cold_gray_blue_dominance = false,
    σ(room_light): reflected = warm(faces_flattered),
    σ(occupant): felt_warmth += f(color_temperature)
  )
```

**APL251 DIFFERENT CHAIRS**
```
NL: "Furnish with a mix of different chairs, each fitting a different body and mood."

→ AB(
    spawn("chair", pos=sitting_places, state={form: varied(size, softness, height)}),
    constraint: matched_identical_set = false,
    σ(sitter): choice = fits(body, mood, moment),
    σ(room): character += accumulated_variety
  )
```

**APL252 POOLS OF LIGHT**
```
NL: "Light rooms with separate warm pools over each social place, not uniform glare."

→ AB ⊗ DE(
    spawn("light_pool", pos=over(table ∪ seat ∪ workspot), state={source: low_warm, spread: local}),
    emit("evening_lighting", time=dusk, target=light_pools),
    σ(group): gathers(within_pool) → intimacy += 0.1,
    constraint: uniform_ceiling_grid = false
  )
```

**APL253 THINGS FROM YOUR LIFE**
```
NL: "Fill the house with things from your own life, not decorator sets."

→ AB(
    spawn("life_thing", pos=shelves ∪ walls ∪ sills, state={origin: lived_history(gift, travel, making)}),
    σ(display): curated_by = accumulation(not_purchase_scheme),
    σ(dwelling): identity = readable(inhabitant_biography),
    σ(occupant): belonging += ∂(surrounded_by_own_story)
  )
```

---

## Paradigm Distribution Summary

| Paradigm | Pattern Count | Percentage | Primary Role |
|----------|-------------|------------|--------------|
| AB only | 121 | 47.8% | Spatial agents, local rules, emergence |
| DE only | 70 | 27.7% | Event scheduling, queues, processes |
| SD only | 11 | 4.3% | Stocks, flows, feedback loops |
| AB ⊗ DE | 23 | 9.1% | Agents triggering/responding to events |
| AB ⊗ SD | 26 | 10.3% | Agents coupled to continuous dynamics |
| DE ⊗ SD | 2 | 0.8% | Events modulating continuous flows |

## Sequence-to-Paradigm Coupling Matrix

```
        AB  DE  SD  AB⊗DE  AB⊗SD  DE⊗SD
seq01    ·   ·   █    ·      ·      ·     Regional stocks
seq02    ▓   ·   █    ·      █      ·     Settlement + dynamics
seq03    █   ·   ▓    ·      █      ·     Cultural agents + flows
seq04    ▓   ·   █    ·      █      ·     Community SD + AB markers
seq05    ·   █   ·    ·      ·      ·     Pure transit processes
seq06    ▓   ·   █    ·      █      ·     Density + sacred agents
seq07    █   ▓   ·    █      ·      ·     Activity + commerce events
seq08    █   ·   ▓    ·      █      ·     Housing agents + demographics
seq09    █   █   ·    █      ·      ·     Work agents + service queues
seq10    █   ·   ·    ·      ·      ·     Pure spatial agents
seq11    █   █   ·    █      ·      ·     Community events
seq12    ▓   ·   █    ·      █      ·     Life/death stocks + agents
seq13    █   ·   ·    ·      ·      ·     Family agents
seq14    █   █   ·    █      ·      ·     Commerce events
seq15    ·   █   ·    ·      ·      ·     Service processes
seq16-28 █   ▓   ·    █      ▓      ·     Building agents + circulation
seq29-36 ·   █   ·    ·      ·      █     Construction processes + material SD

Legend: █ = dominant, ▓ = secondary, · = absent
```

---

## Implementation Interface

Each sequence maps to a `SequenceModule` in the anylogo engine:

```typescript
interface SequenceModule {
  id: number;           // 1-36
  name: string;
  category: 'town' | 'building' | 'construction';
  patterns: PatternId[];
  paradigm: {
    ab: PatternId[];    // Patterns using AB
    de: PatternId[];    // Patterns using DE
    sd: PatternId[];    // Patterns using SD
  };
  initialize(world: World): void;
  update(world: World, dt: number): void;
  metrics(world: World): Record<string, number>;
}
```

---

*Generated by AnyLogo Digital-Twin Engine v0.1.0*
*253 patterns × 36 sequences × 3 paradigms = complete urban simulation grammar*
